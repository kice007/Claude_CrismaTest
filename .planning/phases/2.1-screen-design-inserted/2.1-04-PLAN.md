---
phase: 2.1-screen-design-inserted
plan: "04"
type: execute
wave: 4
depends_on: ["2.1-00", "2.1-01", "2.1-02", "2.1-03"]
files_modified:
  - design/dashboard.pen
autonomous: true
requirements: [DSGN-04]

must_haves:
  truths:
    - "5 desktop dashboard frames exist: dashboard, candidates-detail, compare, talent-pool, build-test"
    - "5 matching mobile frames exist for all 5 views at 375px"
    - "Dashboard desktop frame shows brand-navy sidebar with icon+label nav and candidate table with realistic seeded data"
    - "Mobile dashboard frame shows bottom tab bar (56px, brand-navy) with 5 icons — no sidebar"
    - "Skeleton and EmptyState state variants exist for dashboard table and talent-pool"
    - "Frame count does not exceed 15 (per anti-pattern warning from RESEARCH.md)"
  artifacts:
    - path: "design/dashboard.pen"
      provides: "5 desktop frames + 5 mobile frames + 3 state variant frames = 13 frames total"
      contains: "frames: dashboard, dashboard--mobile, candidates-detail, candidates-detail--mobile, compare, compare--mobile, talent-pool, talent-pool--mobile, build-test, build-test--mobile, dashboard--skeleton, talent-pool--skeleton, dashboard--empty"
  key_links:
    - from: "dashboard.pen InviteModal callout"
      to: "src/components/modals/InviteModal.tsx"
      via: "annotation note on dashboard frame 'Invite Candidate' button"
      pattern: "InviteModal"
    - from: "dashboard.pen Skeleton callout"
      to: "src/components/Skeleton.tsx"
      via: "annotation note on dashboard--skeleton frame"
      pattern: "Skeleton"
---

<objective>
Design all 5 company dashboard views at both desktop and mobile breakpoints, plus state variants for skeleton loading and empty state — 13 frames total in dashboard.pen.

Purpose: DSGN-04 — the dashboard is the most frame-heavy design (5 views × 2 breakpoints + 3 states). Phase 5 implementers need complete visual specs for the sidebar navigation, data tables, candidate detail, compare view, talent pool, and build-test flow before writing any dashboard routes.
Output: design/dashboard.pen with 13 frames (5 desktop + 5 mobile + 3 state variants).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/2.1-screen-design-inserted/2.1-CONTEXT.md
@.planning/phases/2.1-screen-design-inserted/2.1-RESEARCH.md

<!-- Brand tokens (must be re-set in this file):
  brand-primary:   #1B4FD8
  brand-secondary: #3B6FE8
  brand-navy:      #0F2A6B
  brand-light:     #EEF2FF
  brand-accent:    #6366F1
  font-sans:       Inter
  font-mono:       JetBrains Mono
-->

<!-- Frame naming convention for this file:
  "dashboard"               — /dashboard desktop (1440px)
  "dashboard--mobile"       — /dashboard at 375px (bottom tab bar)
  "candidates-detail"       — /dashboard/candidates/[id] desktop
  "candidates-detail--mobile" — same at 375px
  "compare"                 — /dashboard/compare desktop
  "compare--mobile"         — same at 375px
  "talent-pool"             — /dashboard/talent-pool desktop
  "talent-pool--mobile"     — same at 375px
  "build-test"              — /dashboard/build-test desktop (4-step flow)
  "build-test--mobile"      — same at 375px
  "dashboard--skeleton"     — /dashboard loading state (table rows = skeleton shimmer)
  "talent-pool--skeleton"   — /talent-pool loading state (card grid = skeleton shimmer)
  "dashboard--empty"        — /dashboard empty state (no candidates)
-->

<!-- Realistic seeded data to use (per CONTEXT.md + REQUIREMENTS.md):
  Candidate rows: "Marie Dupont — Score 78 — Product Manager", "James Chen — Score 65 — Developer",
                  "Sofia Torres — Score 91 — UX Designer", "Omar Hassan — Score 55 — Sales"
  Dashboard columns: avatar, name, role, CrismaScore chip (brand-accent bg), TrustScore, status badge, date, actions
  Status badges: "Reviewed" (green), "Pending" (amber), "Invited" (brand-primary)
-->

<!-- Phase 2 components to callout in this file:
  InviteModal: "← InviteModal from src/components/modals/InviteModal.tsx"
  CalendarModal: "← CalendarModal from src/components/modals/CalendarModal.tsx"
  ExportModal: "← ExportModal from src/components/modals/ExportModal.tsx"
  Skeleton: "← Skeleton from src/components/Skeleton.tsx (shimmer: true)"
  EmptyState: "← EmptyState from src/components/EmptyState.tsx"
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap dashboard.pen + design dashboard and candidates-detail (desktop + mobile)</name>
  <files>design/dashboard.pen</files>
  <action>
    1. Call mcp__pencil__open_document on "design/dashboard.pen".
    2. Call mcp__pencil__set_variables with all 7 brand tokens.
    3. Call mcp__pencil__get_variables to confirm.

    **Frame 1: "dashboard" — /dashboard desktop (1440px)**
    2-col layout: sidebar (240px, brand-navy bg) + main content area (flex-1, brand-light bg).
    Sidebar contents (top to bottom):
    - LogoMark + "CrismaTest" wordmark (white)
    - Nav items (icon + label, each 48px height): Dashboard (active, brand-primary bg), Candidates, Compare, Talent Pool, Build Test
    - Bottom: user avatar (placeholder circle) + "Acme Corp" + plan badge ("Pro" — brand-accent bg)
    Add annotation: "← NavShell replaced by brand-navy sidebar on dashboard pages; DASH-01"

    Main content area:
    - Page header: "Candidates" title + "Invite Candidate" button (brand-primary, adds annotation "← InviteModal")
    - Filter row: role dropdown, score range slider, status dropdown, date range picker, search input (name or email)
    - Data table: 4 rows using seeded candidate data, columns: avatar | name | role | CrismaScore chip | TrustScore | status badge | date | "View" action link
    - Pagination row: "1–4 of 30 results" + page arrows

    Call mcp__pencil__get_screenshot on "dashboard" and verify.

    **Frame 2: "dashboard--mobile" — /dashboard at 375px**
    Full-screen, no sidebar.
    - Bottom tab bar: 56px height, brand-navy bg, 5 icon-only tabs (Dashboard active, Candidates, Compare, TalentPool, Build — use simple placeholder icons)
    - Add annotation: "Bottom tab bar: 56px, brand-navy, 5 icons — sidebar collapses to this on mobile; DASH-02"
    - Main area above tab bar: page header + search input (full width)
    - Candidate cards (card stack instead of table): each card shows avatar + name + role + CrismaScore chip + status badge, stacked vertically
    - Filter CTA: "Filters" button opens bottom sheet (labeled "← Bottom sheet for filters — Base UI Drawer")

    Call get_screenshot on "dashboard--mobile" and verify.

    **Frame 3: "candidates-detail" — /dashboard/candidates/[id] desktop (1440px)**
    Same sidebar as dashboard frame (same nav, Candidates item active).
    Main area:
    - Header card (brand-light bg, rounded): avatar (64px) + name "Marie Dupont" + role "Product Manager" + email (prominently displayed as mailto link, brand-primary) + test date + status badge
    - Left col: CrismaScore circular gauge (score 78, brand-primary arc) + 5 sub-score bars (Logic 80%, Communication 75%, JobSkill 72%, Trust 70%, Video 68%) labeled with Framer Motion stagger annotation
    - Center col: test timeline (3–4 module timestamps) + mocked video player placeholder "[Video player — mocked preview]"
    - Right col: anti-fraud flags panel (3 items: severity Low/Medium/High badges) + AI insight card (3 bullet interview recommendations)
    - Action buttons: "Invite to Interview" (brand-primary — adds callout "← CalendarModal") + "Email Candidate" (outline, mailto prefilled)
    - Back link: "← Back to Candidates"

    Call get_screenshot on "candidates-detail" and verify.

    **Frame 4: "candidates-detail--mobile" — same at 375px**
    No sidebar. Bottom tab bar at bottom.
    Header card: avatar + name + email as full-width card.
    Score gauge: full-width below header.
    Sub-scores: stacked below gauge.
    Fraud flags and AI insight: stacked cards.
    Action buttons: full-width stacked.

    Call get_screenshot on "candidates-detail--mobile" and verify.
  </action>
  <verify>
    <automated>mcp__pencil__batch_get on design/dashboard.pen — returns frame IDs: dashboard, dashboard--mobile, candidates-detail, candidates-detail--mobile</automated>
  </verify>
  <done>4 frames confirmed. get_screenshot on "dashboard" shows brand-navy sidebar with 5 nav items and data table with Marie Dupont row using seeded data. get_screenshot on "dashboard--mobile" shows 56px bottom tab bar replacing sidebar. get_screenshot on "candidates-detail" shows gauge + sub-scores + AI insight card.</done>
</task>

<task type="auto">
  <name>Task 2: Design compare, talent-pool, build-test (desktop + mobile) + 3 state variant frames</name>
  <files>design/dashboard.pen</files>
  <action>
    **Frame 5: "compare" — /dashboard/compare desktop (1440px)**
    Same sidebar (Compare item active).
    Main area:
    - Header: "Compare Candidates" + dropdown to select candidates (searchable, shows avatar + name options)
    - 3-column layout (equal width): each column = selected candidate card
    - Column card: candidate name header + CrismaScore gauge (smaller, 80px) + 5 sub-score bars + fraud flag count + video thumbnail placeholder
    - "Recommended for Interview" badge: appears on top-scorer column (Marie Dupont, score 91) — brand-accent bg
    - "Export Report" button (outline, top-right): annotation "← ExportModal — downloads static PDF mockup"

    Call get_screenshot on "compare" and verify.

    **Frame 6: "compare--mobile" — same at 375px**
    Bottom tab bar at bottom. Columns stack vertically with candidate name as section header (h3). "Recommended for Interview" badge shows inline below name.

    Call get_screenshot on "compare--mobile" and verify.

    **Frame 7: "talent-pool" — /dashboard/talent-pool desktop (1440px)**
    Same sidebar (Talent Pool item active).
    Main area:
    - Header: "Talent Pool" + search input (name, role, skill) + filter chips row (score range, role, experience, availability)
    - Card grid: 4 columns desktop, each card: avatar + name + role + CrismaScore badge (brand-accent) + TrustScore + last test date + "Contact" CTA
    - Sample cards: Marie Dupont, James Chen, Sofia Torres, Omar Hassan (4 visible cards)
    - Card click: inline detail modal placeholder labeled "← Card click opens inline detail modal"
    - "Contact Candidate" button on card: annotation "← ContactModal from src/components/modals/ContactModal.tsx"

    Call get_screenshot on "talent-pool" and verify.

    **Frame 8: "talent-pool--mobile" — same at 375px**
    Bottom tab bar. Card grid becomes 1-column stack (full-width cards). Filter chips become filter button opening bottom sheet.

    Call get_screenshot on "talent-pool--mobile" and verify.

    **Frame 9: "build-test" — /dashboard/build-test desktop (1440px)**
    Same sidebar (Build Test item active).
    Main area: 4-step flow, step indicators at top (Step 1 active = brand-primary):
    - Step 1 (Role): dropdown with 8 roles (Product Manager, Developer, UX Designer, Sales, Marketing, Data Analyst, HR, Finance)
    - Step 2 (Modules): toggle chip grid for module selection (Logic, Communication, Job Skills, Trust, Video — each chip toggleable)
    - Step 3 (Custom Questions): 3 text input rows, "Add Custom Question" link
    - Step 4 (Generate): "Generate Test" button (brand-primary) → animation placeholder "[Animated loader]" → "Test Ready" confirmation card with "Copy Test Link" + "Send to Candidates" CTAs (both static, outline style)
    Add annotation: "Build-test: 4-step flow, all steps single page — DASH-06"

    Call get_screenshot on "build-test" and verify.

    **Frame 10: "build-test--mobile" — same at 375px**
    Bottom tab bar. Steps collapse to vertical stepper. Module chips 2-per-row. CTAs full-width.

    Call get_screenshot on "build-test--mobile" and verify.

    **Frame 11: "dashboard--skeleton" — /dashboard loading state**
    Exact copy of "dashboard" frame but table rows replaced with skeleton shimmer blocks:
    - Each table row = horizontal shimmer rect (brand-light bg with shimmer animation labeled "[Skeleton shimmer — 1.5s infinite]")
    Add annotation: "← Skeleton from src/components/Skeleton.tsx — composable by className; shown on initial data fetch; CONTEXT.md skeleton spec"

    Call get_screenshot on "dashboard--skeleton" and verify.

    **Frame 12: "talent-pool--skeleton" — /talent-pool loading state**
    Card grid with all cards replaced by skeleton shimmer rectangles (same dimensions as cards, shimmer placeholder).
    Add annotation: "← Skeleton shimmer on talent pool grid during data fetch"

    Call get_screenshot on "talent-pool--skeleton" and verify.

    **Frame 13: "dashboard--empty" — /dashboard empty state (no candidates)**
    Dashboard layout with table area replaced by EmptyState component:
    - Large EmptyState card centered in table area: abstract blue-palette illustration placeholder "[EmptyState illustration]", heading "No candidates yet", body "Invite your first candidate to get started", "Invite Candidate" CTA (brand-primary)
    Add annotation: "← EmptyState from src/components/EmptyState.tsx — shown when candidates table returns empty array"

    Call get_screenshot on "dashboard--empty" and verify.

    Final: call mcp__pencil__batch_get to confirm all 13 frame IDs exist.
    Call mcp__pencil__snapshot_layout on "compare" frame (3-col layout is complex).
  </action>
  <verify>
    <automated>mcp__pencil__batch_get on design/dashboard.pen — returns 13 frame IDs: dashboard, dashboard--mobile, candidates-detail, candidates-detail--mobile, compare, compare--mobile, talent-pool, talent-pool--mobile, build-test, build-test--mobile, dashboard--skeleton, talent-pool--skeleton, dashboard--empty</automated>
  </verify>
  <done>13 frames confirmed in dashboard.pen. get_screenshot on "compare" shows 3-col layout with "Recommended for Interview" badge on top scorer. get_screenshot on "dashboard--skeleton" shows shimmer placeholder rows in table. get_screenshot on "dashboard--empty" shows EmptyState component with annotation. Frame count is 13 — within the 15-frame limit.</done>
</task>

</tasks>

<verification>
- mcp__pencil__batch_get returns exactly 13 frame IDs in dashboard.pen
- mcp__pencil__get_screenshot on "dashboard" shows brand-navy sidebar (240px), seeded candidate data in table rows (Marie Dupont, Score 78)
- mcp__pencil__get_screenshot on "dashboard--mobile" shows 56px bottom tab bar with brand-navy bg replacing the sidebar
- mcp__pencil__get_screenshot on "compare" shows 3-col layout with "Recommended for Interview" badge on highest scorer
- mcp__pencil__snapshot_layout on "compare" reports no critical positioning errors
- Component callout annotations present for InviteModal, CalendarModal, ExportModal, ContactModal, Skeleton, EmptyState
</verification>

<success_criteria>
DSGN-04 complete when: 13 frames exist in dashboard.pen (5 desktop + 5 mobile + 3 state variants), brand-navy sidebar appears on all desktop frames, bottom tab bar appears on all mobile frames, seeded candidate data is used in the candidate table and talent pool, and Phase 2 component callout annotations are on the relevant frames.
</success_criteria>

<output>
After completion, create `.planning/phases/2.1-screen-design-inserted/2.1-04-SUMMARY.md`
</output>

---
phase: 2.1-screen-design-inserted
plan: "03"
type: execute
wave: 3
depends_on: ["2.1-00", "2.1-01", "2.1-02"]
files_modified:
  - design/test-flow.pen
autonomous: true
requirements: [DSGN-03]

must_haves:
  truths:
    - "5 test-flow screen desktop frames exist: intro, check, questions (composite), calculating, result"
    - "6 question format sub-frames exist within questions, each showing a distinct format layout"
    - "1 mobile frame exists for the questions screen (questions--mobile, 375px, full-screen no nav)"
    - "Dynamic content uses labeled placeholder blocks, not images"
    - "Animation annotations appear on calculating and result frames for the 4 specified interactions"
    - "Timer color-change behavior is annotated on the questions frame header"
  artifacts:
    - path: "design/test-flow.pen"
      provides: "5 main screens + 6 question format frames + 1 mobile frame = 12 frames total"
      contains: "frames: intro, check, questions--qcm, questions--dragdrop, questions--casestudy, questions--simulation, questions--audiovideo, questions--shorttext, calculating, result, result--confetti, questions--mobile"
  key_links:
    - from: "test-flow.pen result frame"
      to: "CrismaScore gauge animation spec"
      via: "note annotation: '0→final, 1.5s ease-out, driven by Framer Motion scaleIn from src/lib/motion.ts'"
      pattern: "gauge.*scaleIn"
    - from: "test-flow.pen calculating frame"
      to: "staggered step sequence spec"
      via: "note annotation listing 4 steps with timing"
      pattern: "calculating.*stagger"
---

<objective>
Design the complete candidate test flow in test-flow.pen: 5 main screens, all 6 question format layouts as separate frames, the mobile questions frame, and a confetti-state result variant — 12 frames total.

Purpose: DSGN-03 — the test flow is the most complex screen sequence in the product. Phase 4 implementers need a complete visual spec for all 6 question formats and all animation states before writing any test engine code.
Output: design/test-flow.pen with 12 frames covering the full candidate journey from intro through result.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/2.1-screen-design-inserted/2.1-CONTEXT.md
@.planning/phases/2.1-screen-design-inserted/2.1-RESEARCH.md

<!-- Brand tokens (must be re-set in this file):
  brand-primary:   #1B4FD8
  brand-secondary: #3B6FE8
  brand-navy:      #0F2A6B
  brand-light:     #EEF2FF
  brand-accent:    #6366F1
  font-sans:       Inter
  font-mono:       JetBrains Mono
-->

<!-- Frame naming convention for this file:
  "intro"                — /test/[id]/intro desktop
  "check"                — /test/[id]/check (pre-flight) desktop
  "questions--qcm"       — /questions with QCM format (A/B/C/D single-select)
  "questions--dragdrop"  — /questions with drag & drop ranking
  "questions--casestudy" — /questions with case study format
  "questions--simulation"— /questions with simulation (branching scenario)
  "questions--audiovideo"— /questions with audio/video recording format
  "questions--shorttext" — /questions with short text (150-word limit)
  "calculating"          — /test/[id]/calculating full-screen animated
  "result"               — /test/[id]/result (score below 70, no confetti)
  "result--confetti"     — /test/[id]/result with score > 70 (confetti state)
  "questions--mobile"    — /questions at 375px, full-screen no nav
-->

<!-- Test requirements (from REQUIREMENTS.md):
  TEST-01: intro — role title, estimated time 10–15 min, module breakdown, anti-fraud notice, "Start Test" CTA
  TEST-02: check — webcam preview [placeholder], mic level bars [animated placeholder], anti-fraud checklist, disclaimer checkbox (disables CTA), "I'm Ready" CTA
  TEST-03: questions — progress bar + module badge + countdown timer (JetBrains Mono, amber at 1min, red at 30s); 6 formats; Previous/Next + flag for review; full-screen on mobile
  TEST-04: calculating — 5-7s animated sequence, staggered steps, gauge fill, particle bg [placeholder]
  TEST-05: result — CrismaScore gauge, 5 sub-score bars, strengths panel, confetti if > 70; share CTA; LinkedIn sheet
-->

<!-- Animation annotations required (CONTEXT.md spec):
  Score gauge: "0→final score, 1.5s ease-out, driven by Framer Motion scaleIn from src/lib/motion.ts"
  Calculating steps: "'Analyzing responses.' → 'Checking consistency.' → 'Computing sub-scores.' → 'Generating CrismaScore.' — staggerChildren 0.3s"
  Confetti: "canvas-confetti fires when score > 70 — see result--confetti frame"
  Timer: "JetBrains Mono — amber (#F59E0B) at 1:00 remaining, red (#EF4444) at 0:30 remaining"
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap test-flow.pen + design intro and check screens</name>
  <files>design/test-flow.pen</files>
  <action>
    1. Call mcp__pencil__open_document on "design/test-flow.pen".
    2. Call mcp__pencil__set_variables with all 7 brand tokens.
    3. Call mcp__pencil__get_variables to confirm.

    **Frame 1: "intro" — /test/[id]/intro desktop (1440px)**
    Full-width centered card layout, brand-light page bg.
    Card contents:
    - Role title badge (e.g., "Product Manager Assessment") — brand-accent bg, white text
    - Estimated time row: clock icon + "10–15 minutes"
    - Module breakdown: 3–4 rows, each with icon + label + estimated time (e.g., "Logic & Reasoning — 4 min", "Communication — 3 min")
    - Anti-fraud notice: small text with shield icon, "This test uses webcam monitoring"
    - "Start Test" CTA: brand-primary, full width
    Add annotation: "← NavShell absent on test pages — full-screen focus mode"

    Call mcp__pencil__get_screenshot on "intro" frame and verify.

    **Frame 2: "check" — /test/[id]/check desktop (1440px)**
    Full-width 2-col layout (left: camera/mic checks, right: checklist).
    Left column:
    - Webcam preview area: large rect labeled "[Webcam preview — browser getUserMedia()]"
    - Mic level indicator: 5 animated bars labeled "[Mic level bars — Web Audio API, animated]"
    Right column:
    - Anti-fraud checklist: 4 items with checkboxes (camera, mic, alone in room, no extra screens)
    - Disclaimer checkbox: "I agree to the testing conditions" (required before CTA activates)
    - "I'm Ready" CTA: brand-primary, disabled state (opacity 50%) with note "Activates only when all checkboxes checked"
    - Camera denied warning: red banner "[Camera/mic denied — show retry button]" as annotation
    Add note: "Pre-flight check: real webcam/mic permission request via browser API — TEST-02"

    Call mcp__pencil__get_screenshot on "check" frame and verify.
  </action>
  <verify>
    <automated>mcp__pencil__get_variables on design/test-flow.pen — returns 7 brand tokens; mcp__pencil__batch_get returns frame IDs: intro, check</automated>
  </verify>
  <done>Brand tokens set. Frames "intro" and "check" exist. get_screenshot on "check" shows webcam placeholder, mic bars placeholder, checklist with 4 items, and disabled CTA with annotation.</done>
</task>

<task type="auto">
  <name>Task 2: Design all 6 question format frames + mobile questions frame</name>
  <files>design/test-flow.pen</files>
  <action>
    All question frames share the same outer shell — header with progress bar + module badge + countdown timer, footer with Previous/Next navigation + flag-for-review. The format-specific content changes in the main body area. Full-screen layout (no NavShell), brand-light bg.

    **Shared questions shell elements (define once, reference in each frame):**
    - Header: progress bar (brand-primary fill, percentage label), module badge (brand-accent bg), countdown timer (JetBrains Mono, "12:34" example time)
    - Add annotation on header: "Timer: JetBrains Mono — color #F59E0B (amber) at 1:00 remaining, #EF4444 (red) at 0:30 remaining"
    - Footer: "← Previous" button (outline) + "Next →" button (brand-primary) + "Flag for Review" link (brand-accent)

    **Frame 3: "questions--qcm" — QCM single-select (1440px)**
    Body: question text + 4 option cards (A/B/C/D), option B shown as selected state (brand-primary border + light fill). Add note: "Multi-select variant: checkboxes instead of radio; per TEST-03"

    Call get_screenshot and verify.

    **Frame 4: "questions--dragdrop" — drag & drop ranking (1440px)**
    Body: question text + 4 draggable items as cards with grab handle icons, stacked vertically in numbered slots. Items labeled "Item 1", "Item 2" etc with realistic content (e.g., "Prioritize urgent client request"). Add note: "dnd-kit — 48px touch targets; drag handle visible per TEST-03"

    Call get_screenshot and verify.

    **Frame 5: "questions--casestudy" — case study (1440px)**
    Body: scenario text block (3–4 sentences, brand-light bg card), then 3 decision sub-questions each with QCM options. Add note: "Case study: scenario + 3 decision questions — TEST-03"

    Call get_screenshot and verify.

    **Frame 6: "questions--simulation" — branching scenario (1440px)**
    Body: customer support scenario card (2-col: situation left, response options right), with 2–3 branching choice buttons. Add note: "Simulation: branching customer support scenario — answer changes next question — TEST-03"

    Call get_screenshot and verify.

    **Frame 7: "questions--audiovideo" — audio/video recording (1440px)**
    Body: question text, large "[Video recording area — camera feed]" placeholder rect, 90s countdown bar labeled "[Recording countdown 90s]", "Start Recording" and "Preview Before Submit" buttons. Add note: "90s record limit; preview before submit; TEST-03"

    Call get_screenshot and verify.

    **Frame 8: "questions--shorttext" — short text (1440px)**
    Body: question text, textarea with character counter "0 / 150 words" (JetBrains Mono for counter), "Clear" + "Submit Answer" buttons. Add note: "150-word limit; real-time counter in JetBrains Mono — TEST-03"

    Call get_screenshot and verify.

    **Frame 9: "questions--mobile" — QCM format at 375px, full-screen**
    Full-screen, no NavShell, no padding wasted on desktop sidebar.
    - Header: progress bar (full width), timer (JetBrains Mono, right-aligned)
    - Body: QCM question + 4 option cards stacked vertically, full width
    - Footer: Previous / Next / Flag full-width row, 48px tap targets
    Add note: "Full-screen mobile test engine — no nav, no chrome except progress + timer; CONTEXT.md mobile spec"

    Call get_screenshot on "questions--mobile" and verify 375px layout.
  </action>
  <verify>
    <automated>mcp__pencil__batch_get on design/test-flow.pen — returns frame IDs including: questions--qcm, questions--dragdrop, questions--casestudy, questions--simulation, questions--audiovideo, questions--shorttext, questions--mobile</automated>
  </verify>
  <done>7 question-related frames exist. get_screenshot on "questions--dragdrop" shows draggable cards with grab handles. get_screenshot on "questions--audiovideo" shows video recording placeholder area. get_screenshot on "questions--mobile" shows full-screen 375px layout with no nav chrome.</done>
</task>

<task type="auto">
  <name>Task 3: Design calculating and result screens with animation annotations</name>
  <files>design/test-flow.pen</files>
  <action>
    **Frame 10: "calculating" — /test/[id]/calculating full-screen (1440px)**
    Full-screen, brand-navy bg.
    Center content:
    - Neural-network particle background: large area labeled "[Neural-network particle animation bg]"
    - 4 stacked step labels with stagger: "Analyzing responses." | "Checking consistency." | "Computing sub-scores." | "Generating CrismaScore." — each with a progress dot/check icon
    - Animated gauge fill area: "[Gauge fill animation — fills as steps complete]"
    Add required annotation note: "Calculating sequence: staggerChildren 0.3s — 'Analyzing responses.' → 'Checking consistency.' → 'Computing sub-scores.' → 'Generating CrismaScore.' — auto-redirects to /result after 5–7s"

    Call get_screenshot on "calculating" and verify.

    **Frame 11: "result" — /test/[id]/result desktop, score ≤ 70 (1440px)**
    Layout: 2-col (left: score gauge, right: sub-scores + strengths).
    Left column:
    - CrismaScore circular gauge: large, brand-primary arc, center shows "74" in Inter bold, grade badge "Good" below
    - Labeled placeholder: "[Gauge animates 0→74, 1.5s ease-out]"
    Add required annotation: "Gauge: 0→final score, 1.5s ease-out, Framer Motion scaleIn from src/lib/motion.ts"
    Right column:
    - 5 animated sub-score bars: Logic (80%), Communication (70%), JobSkill (72%), Trust (65%), Video (78%) — brand-primary fill bars with stagger
    - Strengths panel: 2 bullet points (e.g., "Strong logical reasoning", "Clear written communication")
    Footer actions:
    - "Share Your CrismaScore" CTA (brand-primary) — opens LinkedIn sheet labeled "← LinkedIn share sheet"
    - "Improve Your Score" link → /pricing
    - "Retake Test" link with retest counter note "retest counter shown"

    Call get_screenshot on "result" and verify.

    **Frame 12: "result--confetti" — /test/[id]/result with score > 70 (1440px)**
    Same layout as "result" but:
    - Score shows "85", grade badge "Excellent"
    - Canvas confetti overlay: labeled "[canvas-confetti fires on mount — covers viewport briefly]"
    Add required annotation: "Confetti: canvas-confetti fires when score > 70 — brief viewport overlay"

    Call get_screenshot on "result--confetti" and verify.

    Final verification: call mcp__pencil__batch_get to confirm all 12 frame IDs exist.
    Call mcp__pencil__snapshot_layout on "result" frame to check for positioning issues.
  </action>
  <verify>
    <automated>mcp__pencil__batch_get on design/test-flow.pen — returns 12 frame IDs: intro, check, questions--qcm, questions--dragdrop, questions--casestudy, questions--simulation, questions--audiovideo, questions--shorttext, calculating, result, result--confetti, questions--mobile</automated>
  </verify>
  <done>12 frames confirmed in test-flow.pen. get_screenshot on "calculating" shows brand-navy bg with staggered step labels and animation annotation note. get_screenshot on "result" shows 2-col layout with gauge (score 74) and 5 sub-score bars. get_screenshot on "result--confetti" shows confetti placeholder overlay with score 85. All 4 required animation annotations are present as note nodes.</done>
</task>

</tasks>

<verification>
- mcp__pencil__batch_get returns all 12 frame IDs in test-flow.pen
- mcp__pencil__get_screenshot on "questions--qcm" shows header with progress bar + JetBrains Mono timer + all question formats have distinct layouts
- mcp__pencil__get_screenshot on "questions--mobile" shows 375px full-screen layout with no NavShell
- Animation note annotations present on: calculating (stagger sequence), result (gauge 0→final 1.5s), result--confetti (canvas-confetti trigger), questions--qcm header (timer color change)
- mcp__pencil__snapshot_layout on "result" reports no critical positioning errors
</verification>

<success_criteria>
DSGN-03 complete when: 12 frames exist in test-flow.pen (5 screens + 6 question format frames + 1 mobile), all 6 question format layouts are visually distinct, all 4 required animation annotations are present as note nodes, and dynamic content uses labeled placeholder blocks throughout.
</success_criteria>

<output>
After completion, create `.planning/phases/2.1-screen-design-inserted/2.1-03-SUMMARY.md`
</output>

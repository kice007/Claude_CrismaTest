---
phase: 2.1-screen-design-inserted
plan: "05"
type: execute
wave: 5
depends_on: ["2.1-00", "2.1-04"]
files_modified:
  - design/payments.pen
autonomous: true
requirements: [DSGN-05]

must_haves:
  truths:
    - "2 payment desktop frames exist in payments.pen: checkout and checkout-success"
    - "Checkout frame shows the Stripe-styled card form with plan summary sidebar and billing toggle"
    - "Annual billing toggle annotation specifies live price switching behavior"
    - "No mobile-specific frames needed for payment screens (per CONTEXT.md locked decision)"
  artifacts:
    - path: "design/payments.pen"
      provides: "2 payment flow desktop frames"
      contains: "frames: checkout, checkout-success"
  key_links:
    - from: "payments.pen checkout frame billing toggle"
      to: "pricing page annual toggle spec in landing-pages.pen"
      via: "annotation: 'mirrors pricing page toggle behavior — price updates live'"
      pattern: "billing.*toggle"
---

<objective>
Design the checkout and checkout success screens in payments.pen. This is the smallest design plan in Phase 2.1 — two focused frames covering the visual-only payment flow that Phase 6 implements.

Purpose: DSGN-05 — the Stripe-styled checkout form has a specific layout (plan summary card + form + billing toggle) that needs a visual spec so Phase 6 builds it correctly without guessing the column layout or form field grouping.
Output: design/payments.pen with 2 frames.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/2.1-screen-design-inserted/2.1-CONTEXT.md
@.planning/phases/2.1-screen-design-inserted/2.1-RESEARCH.md

<!-- Brand tokens (must be re-set in this file):
  brand-primary:   #1B4FD8
  brand-secondary: #3B6FE8
  brand-navy:      #0F2A6B
  brand-light:     #EEF2FF
  brand-accent:    #6366F1
  font-sans:       Inter
  font-mono:       JetBrains Mono
-->

<!-- Frame naming convention:
  "checkout"         — /checkout/[plan] desktop (1440px)
  "checkout-success" — /checkout/success desktop
-->

<!-- Payment requirements (from REQUIREMENTS.md):
  PAY-01: /checkout/[plan] — plan summary card (name, price, features); billing toggle (monthly/annual, switches price display);
          Stripe-styled card form (card number, expiry, CVC, cardholder name); billing info (email, address, country);
          "Subscribe Now" → 1.5s loading → /checkout/success; "Back to Pricing" link
  PAY-02: /checkout/success — success illustration, "Welcome to CrismaTest [Plan]!" headline,
          "Go to Dashboard" → /dashboard, "Take Your First Test" → /test/sample/intro
-->

<!-- No mobile frames for payment screens per CONTEXT.md locked decision:
  "Screens that don't need separate mobile frames: auth forms, pricing, checkout, home page sections
  (handled via Tailwind breakpoints, behavior defined in REQUIREMENTS.md)"
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap payments.pen + design both payment frames</name>
  <files>design/payments.pen</files>
  <action>
    1. Call mcp__pencil__open_document on "design/payments.pen".
    2. Call mcp__pencil__set_variables with all 7 brand tokens.
    3. Call mcp__pencil__get_variables to confirm all 7 tokens present.

    **Frame 1: "checkout" — /checkout/[plan] desktop (1440px)**
    2-col layout: left col (plan summary, 380px) + right col (form area, flex-1). Page bg: brand-light.

    Left column — Plan Summary Card (white bg, rounded-lg, shadow-sm, sticky):
    - Plan name badge: "Pro Plan" (brand-accent bg, white text)
    - Price display: "$149/mo" (large Inter bold, brand-navy)
    - Billing toggle: "Monthly | Annual" toggle (Annual selected state shows "$119/mo" with "Save 20%" badge in brand-accent)
    - Add annotation: "Billing toggle: price updates live — $149/mo ↔ $119/mo; mirrors pricing page toggle behavior; PAY-01"
    - Feature list: 3–4 checkmark rows (Inter, brand-navy text): "Unlimited assessments", "Advanced analytics", "Priority support", "API access"
    - "Back to Pricing" link (brand-primary, below card)

    Right column — Payment Form (white bg, rounded-lg, shadow-sm):
    - Section heading: "Payment details" (Inter semibold)
    - Stripe-styled card form group:
      - Card number field: full-width input with card brand icon placeholder on right
      - 2-col row: Expiry date (MM/YY) | CVC (3-digit)
      - Cardholder name: full-width input
    - Section heading: "Billing information"
    - Billing info fields:
      - Email: full-width input
      - Address: full-width input
      - 2-col row: City | Postal Code
      - Country: dropdown (default "France")
    - "Subscribe Now" CTA: full-width, brand-primary, large
    - Add annotation: "'Subscribe Now' → 1.5s loading state → /checkout/success (visual only — no real Stripe integration in v1)"
    - Secure payment note: lock icon + "Secured by Stripe" text (small, brand-navy/60)

    Call mcp__pencil__get_screenshot on "checkout" and verify.

    **Frame 2: "checkout-success" — /checkout/success desktop (1440px)**
    Centered layout, brand-light bg, white card (max-width 560px, rounded-lg, shadow-md).
    Card contents:
    - Success illustration: large checkmark circle or celebration graphic labeled "[Success illustration placeholder]" (brand-primary or brand-accent color)
    - Heading: "Welcome to CrismaTest Pro!" (Inter bold, brand-navy)
    - Sub-text: "Your subscription is active. You're all set to start hiring smarter."
    - Two CTA buttons (stacked):
      - "Go to Dashboard" (brand-primary, full-width) → /dashboard
      - "Take Your First Test" (outline, brand-navy, full-width) → /test/sample/intro

    Call mcp__pencil__get_screenshot on "checkout-success" and verify.

    Final: call mcp__pencil__batch_get to confirm both frame IDs: checkout, checkout-success.
  </action>
  <verify>
    <automated>mcp__pencil__batch_get on design/payments.pen — returns both frame IDs: checkout, checkout-success</automated>
  </verify>
  <done>2 frames confirmed in payments.pen. get_screenshot on "checkout" shows 2-col layout with plan summary card on left (featuring billing toggle annotation) and Stripe-styled card form on right with "Subscribe Now" CTA. get_screenshot on "checkout-success" shows centered success card with both CTAs.</done>
</task>

</tasks>

<verification>
- mcp__pencil__batch_get on design/payments.pen returns 2 frame IDs: checkout, checkout-success
- mcp__pencil__get_screenshot on "checkout" shows left plan summary column with billing toggle (monthly/annual) and right column Stripe-styled form (card number, expiry, CVC fields)
- mcp__pencil__get_screenshot on "checkout-success" shows centered card with success illustration and 2 CTAs
- Billing toggle annotation present on checkout frame specifying live price switching
</verification>

<success_criteria>
DSGN-05 complete when: 2 frames exist in payments.pen, the checkout frame clearly shows the 2-col layout with Stripe-styled card form and billing toggle, and the success frame shows the welcome headline with both post-checkout navigation CTAs.
</success_criteria>

<output>
After completion, create `.planning/phases/2.1-screen-design-inserted/2.1-05-SUMMARY.md`
</output>

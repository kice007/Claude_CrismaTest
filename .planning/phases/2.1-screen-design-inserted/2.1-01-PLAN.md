---
phase: 2.1-screen-design-inserted
plan: "01"
type: execute
wave: 1
depends_on: ["2.1-00"]
files_modified:
  - design/landing-pages.pen
autonomous: true
requirements: [DSGN-01]

must_haves:
  truths:
    - "All 5 landing page desktop frames exist in landing-pages.pen (home, for-candidates, for-companies, pricing, contact)"
    - "One mobile nav frame exists showing the hamburger sheet open state at 375px"
    - "Brand tokens are set as variables in landing-pages.pen before any frame is drawn"
    - "NavShell component is marked reusable in the file so it can be referenced by auth.pen and test-flow.pen"
    - "Component callout annotations label NavShell, ContactModal, and skeleton states directly on frames"
  artifacts:
    - path: "design/landing-pages.pen"
      provides: "5 landing page desktop frames + 1 mobile nav frame + NavShell reusable component"
      contains: "frames: home, for-candidates, for-companies, pricing, contact, nav--mobile"
  key_links:
    - from: "landing-pages.pen NavShell component"
      to: "auth.pen and test-flow.pen"
      via: "cross-file import — NavShell must be finalized here before other files reference it"
      pattern: "reusable.*NavShell"
---

<objective>
Design all 5 landing pages and nav states in landing-pages.pen using Pencil MCP tools. This produces the visual spec that Phase 3 implementers reference before building any landing page routes.

Purpose: DSGN-01 — landing pages are the first thing Phase 3 builds; they need a complete visual reference before any code is written. The NavShell reusable component defined here also feeds into auth.pen and test-flow.pen.
Output: design/landing-pages.pen with 6 frames (5 desktop landing pages + 1 mobile nav) and NavShell marked as reusable.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/2.1-screen-design-inserted/2.1-CONTEXT.md
@.planning/phases/2.1-screen-design-inserted/2.1-RESEARCH.md

<!-- Brand tokens (from globals.css — confirmed values):
  brand-primary:   #1B4FD8  — buttons, links, active states
  brand-secondary: #3B6FE8  — secondary interactive elements
  brand-navy:      #0F2A6B  — hero bg, dark surfaces
  brand-light:     #EEF2FF  — page background, card tints
  brand-accent:    #6366F1  — accent badges, highlights
  font-sans:       Inter
  font-mono:       JetBrains Mono
-->

<!-- Frame naming convention for this file:
  "home"              — / desktop (1440px wide)
  "for-candidates"    — /for-candidates desktop
  "for-companies"     — /for-companies desktop
  "pricing"           — /pricing desktop
  "contact"           — /contact desktop
  "nav--mobile"       — hamburger sheet open state at 375px
  "nav-shell"         — NavShell reusable component (marked reusable: true)
-->

<!-- Phase 3 locked content decisions to use:
  Hero headline: "Find top talent with confidence" (or equivalent from Phase 3 CONTEXT)
  Pricing plans: Free / Starter $49 / Pro $149 / Enterprise
  Pro card: taller, brand-primary top border, "Most Popular" badge
  Annual billing toggle: shows 20% discount (e.g., $49 → $39/mo)
  Testimonials: 3-col desktop / carousel mobile
  FAQ: 6–8 items
  Dashboard hero card: floating simplified candidate table mockup, frosted/blurred edges
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap landing-pages.pen — open document, set brand variables, verify tokens</name>
  <files>design/landing-pages.pen</files>
  <action>
    Follow the mandatory per-file initialization sequence:

    1. Call mcp__pencil__open_document with filePath "design/landing-pages.pen" to open the file in Pencil.

    2. Call mcp__pencil__set_variables with all brand tokens:
       - brand-primary: #1B4FD8
       - brand-secondary: #3B6FE8
       - brand-navy: #0F2A6B
       - brand-light: #EEF2FF
       - brand-accent: #6366F1
       - font-sans: Inter
       - font-mono: JetBrains Mono

    3. Call mcp__pencil__get_variables to confirm the 7 tokens are now present in the file.

    4. Create the NavShell reusable component frame first (before any page frames) using mcp__pencil__batch_design:
       - Node: id "nav-shell", type "frame", label "NavShell", reusable: true, width 1440, height 72
       - Layout: horizontal, brand-light fill at 95% opacity (glassmorphism), shadow-sm on scroll annotation
       - Contents: LogoMark left (placeholder rect labeled "← LogoMark from src/components/nav/LogoMark.tsx"), nav links center (Home, For Candidates, For Companies, Pricing, Contact), right group (EN|FR pills + Login button + Sign Up button in brand-primary)
       - Add a note annotation: "← NavShell from src/components/nav/NavShell.tsx — white/95 backdrop-blur, shadow on scroll"

    5. Call mcp__pencil__get_screenshot on "nav-shell" node to verify the desktop nav visual.
  </action>
  <verify>
    <automated>mcp__pencil__get_variables on design/landing-pages.pen — returns 7 brand tokens including brand-primary: #1B4FD8</automated>
  </verify>
  <done>Brand tokens are set in landing-pages.pen. NavShell reusable component frame exists and get_screenshot shows the nav bar with brand-primary Sign Up button.</done>
</task>

<task type="auto">
  <name>Task 2: Design all 5 landing page desktop frames + mobile nav frame</name>
  <files>design/landing-pages.pen</files>
  <action>
    Design 6 frames in sequence using mcp__pencil__batch_design. After each frame is complete, call mcp__pencil__get_screenshot to verify before moving to the next. Use brand token variables (not hardcoded hex values) for all fills and strokes.

    **Frame 1: "home" — / desktop (1440px)**
    Sections top-to-bottom (vertical layout, gap 0):
    - NavShell ref (instance of nav-shell reusable component)
    - Hero section: brand-navy bg, gradient overlay, large headline (Inter bold, white), sub-headline, two CTAs (brand-primary "Get Started" + outline "Watch Demo"), floating dashboard card (simplified candidate table mockup, frosted glass effect — labeled "[Floating dashboard card — frosted edges]")
    - Trust bar: marquee placeholder with 5–6 company logo shapes, labeled "[Trust bar — company logos marquee]"
    - What-is section: 2-col layout, headline + body text left, visual right
    - 3-step process: numbered steps (brand-accent circles), icons + labels
    - For-companies feature grid: 3-col cards, icons + headlines + body
    - Anti-fraud section: brand-navy bg, security badge icons + callouts
    - Test library preview: cards grid showing sample test types
    - Testimonials: 3-col cards (avatars, quotes, names/roles)
    - FAQ: accordion list, 6 items visible
    - Footer: brand-navy bg, logo + links columns + copyright
    Add component callout note: "← NavShell, ← EmptyState (empty-state variant in data sections)"

    **Frame 2: "for-candidates" — /for-candidates desktop (1440px)**
    Sections:
    - NavShell ref
    - Hero: CrismaScore breakdown visual (circular gauge labeled "[CrismaScore gauge]"), headline, body
    - Share examples section: social card mockups labeled "[LinkedIn share card mockup]"
    - Social proof: testimonials from candidates
    - "Take the Test" CTA section: brand-primary bg, headline + button

    **Frame 3: "for-companies" — /for-companies desktop (1440px)**
    Sections:
    - NavShell ref
    - Hero: B2B headline, dashboard screenshot placeholder labeled "[Dashboard screenshot]"
    - Speed / Accuracy / Anti-fraud 3-col feature cards
    - Company logos trust bar
    - Two CTAs: "Request Demo" (brand-primary) + "Start Trial" (outline)

    **Frame 4: "pricing" — /pricing desktop (1440px)**
    Sections:
    - NavShell ref
    - Heading + monthly/annual toggle (shows 20% discount, annotate: "Annual toggle: $49 → $39/mo live price update")
    - 4-plan card row: Free | Starter $49 | Pro $149 (taller, brand-primary top border, "Most Popular" badge) | Enterprise
    - Each card: plan name, price, feature list, CTA button ("Get Started" on paid plans linking to /checkout/[plan])
    Add annotation note: "Pro card: taller height, brand-primary 4px top border, 'Most Popular' badge — per locked spec"

    **Frame 5: "contact" — /contact desktop (1440px)**
    Sections:
    - NavShell ref
    - Demo request form: name, company, email, team size dropdown, message textarea, consent checkbox, "Send Message" CTA (brand-primary, disabled until consent checked)
    - Form error state variant inline: red text below email field labeled "[Inline validation error state]"
    - Success toast callout: label "← Toast success from src/components/ui/sonner — fires on submit"
    Add annotation note: "POST to Supabase contact_submissions + Resend email; rate limited 3/IP/hour"

    **Frame 6: "nav--mobile" — mobile nav open state (375px)**
    - Show hamburger icon (top-right) as "open" state
    - Slide-down sheet open: all nav links stacked vertically, 48px tap targets, language toggle (globe icon + EN/FR), Login + Sign Up at bottom
    - Sheet bg: white, nav-navy accent on active link
    Add annotation note: "← NavMobile from src/components/nav/NavMobile.tsx — Base UI Sheet, 48px tap targets"

    After all 6 frames: call mcp__pencil__batch_get to confirm all 6 frame IDs (home, for-candidates, for-companies, pricing, contact, nav--mobile) exist in the file.
    Call mcp__pencil__snapshot_layout on "home" frame to check for positioning issues.
  </action>
  <verify>
    <automated>mcp__pencil__batch_get on design/landing-pages.pen — returns nodes including frame IDs: home, for-candidates, for-companies, pricing, contact, nav--mobile, nav-shell</automated>
  </verify>
  <done>All 6 frames exist in landing-pages.pen. get_screenshot on each frame shows brand-primary blue buttons, brand-navy hero/footer backgrounds, Inter typography, and realistic content. Pricing Pro card is visually taller with top border accent. NavShell ref appears at top of each landing page frame.</done>
</task>

</tasks>

<verification>
- mcp__pencil__batch_get on design/landing-pages.pen returns 7 node IDs: nav-shell, home, for-candidates, for-companies, pricing, contact, nav--mobile
- mcp__pencil__get_variables returns all 7 brand tokens with correct hex values
- mcp__pencil__get_screenshot on "pricing" frame shows the Pro card as visually taller with "Most Popular" badge
- mcp__pencil__get_screenshot on "nav--mobile" frame shows the sheet open with stacked links at 375px
- mcp__pencil__snapshot_layout on "home" reports no critical positioning errors
</verification>

<success_criteria>
DSGN-01 complete when: 5 landing page desktop frames + 1 mobile nav frame exist in landing-pages.pen, brand tokens are set as variables, NavShell is marked reusable, and screenshots confirm brand-primary/brand-navy coloring with realistic Phase 3 content.
</success_criteria>

<output>
After completion, create `.planning/phases/2.1-screen-design-inserted/2.1-01-SUMMARY.md`
</output>

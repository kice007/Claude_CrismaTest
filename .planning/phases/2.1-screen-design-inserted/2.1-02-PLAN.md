---
phase: 2.1-screen-design-inserted
plan: "02"
type: execute
wave: 2
depends_on: ["2.1-00", "2.1-01"]
files_modified:
  - design/auth.pen
autonomous: true
requirements: [DSGN-02]

must_haves:
  truths:
    - "Three auth screen desktop frames exist in auth.pen: sign-up, login, forgot-password"
    - "Sign-up frame shows role selector (Candidate / Company) and Google OAuth button with 'Coming soon' label"
    - "Form error state variant exists for sign-up and login (inline field error + disabled CTA)"
    - "Brand tokens are set in auth.pen before any frame is drawn"
    - "NavShell is referenced from landing-pages.pen (not rebuilt) via component callout annotation"
  artifacts:
    - path: "design/auth.pen"
      provides: "3 auth desktop frames + 2 error state variants"
      contains: "frames: sign-up, login, forgot-password, sign-up--error, login--error"
  key_links:
    - from: "auth.pen NavShell usage"
      to: "landing-pages.pen nav-shell component"
      via: "annotation callout — implementers import NavShell from nav, not rebuild"
      pattern: "nav-shell.*NavShell"
---

<objective>
Design the three auth screens and their error state variants in auth.pen. This spec is used by Phase 4 implementers before building /sign-up, /login, and /forgot-password.

Purpose: DSGN-02 — auth screens have role selector logic, show/hide password, and OAuth placement that needs a visual spec so Phase 4 builds it correctly first time.
Output: design/auth.pen with 5 frames (3 base screens + 2 error state variants).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/2.1-screen-design-inserted/2.1-CONTEXT.md
@.planning/phases/2.1-screen-design-inserted/2.1-RESEARCH.md

<!-- Brand tokens (same as landing-pages.pen — must be re-set in this file):
  brand-primary:   #1B4FD8
  brand-secondary: #3B6FE8
  brand-navy:      #0F2A6B
  brand-light:     #EEF2FF
  brand-accent:    #6366F1
  font-sans:       Inter
  font-mono:       JetBrains Mono
-->

<!-- Frame naming convention for this file:
  "sign-up"        — /sign-up desktop (centered card layout)
  "login"          — /login desktop
  "forgot-password"— /forgot-password desktop (static confirmation)
  "sign-up--error" — /sign-up with form validation error state
  "login--error"   — /login with form validation error state
-->

<!-- Auth requirements (from REQUIREMENTS.md):
  AUTH-01: sign-up — full name, email, password show/hide, role selector Candidate/Company;
           Google OAuth button shows "Coming soon" toast; submit → redirect per role
  AUTH-02: login — email + password + remember me checkbox; forgot password link → /forgot-password
  AUTH-03: forgot-password — static "Check your email" confirmation screen
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap auth.pen — open document, set brand variables, verify tokens</name>
  <files>design/auth.pen</files>
  <action>
    Follow the mandatory per-file initialization sequence:

    1. Call mcp__pencil__open_document with filePath "design/auth.pen".

    2. Call mcp__pencil__set_variables with all 7 brand tokens:
       - brand-primary: #1B4FD8
       - brand-secondary: #3B6FE8
       - brand-navy: #0F2A6B
       - brand-light: #EEF2FF
       - brand-accent: #6366F1
       - font-sans: Inter
       - font-mono: JetBrains Mono

    3. Call mcp__pencil__get_variables to confirm all 7 tokens are present.

    Note: Variables are not shared across .pen files — this re-set is required even though landing-pages.pen was already initialized.
  </action>
  <verify>
    <automated>mcp__pencil__get_variables on design/auth.pen — returns 7 brand tokens</automated>
  </verify>
  <done>Brand tokens confirmed in auth.pen. get_variables returns brand-primary: #1B4FD8 and all 6 other tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Design 3 auth frames + 2 error state variants</name>
  <files>design/auth.pen</files>
  <action>
    Design 5 frames using mcp__pencil__batch_design. After each frame, call mcp__pencil__get_screenshot to verify before continuing.

    **Frame 1: "sign-up" — /sign-up desktop**
    Layout: centered card (max-width 480px, white bg, rounded-lg, shadow-md) on brand-light page background.
    Card contents top to bottom:
    - LogoMark (placeholder rect labeled "← LogoMark from src/components/nav/LogoMark.tsx")
    - Heading: "Create your account" (Inter bold, brand-navy)
    - Role selector: two toggle buttons side-by-side, "Candidate" and "Company" — "Candidate" selected state (brand-primary bg, white text); Company = outline
    - Form fields: Full Name (text input), Email (text input), Password (text input with eye icon toggle on right, labeled "show/hide password")
    - "Create Account" CTA button: brand-primary, full width
    - Divider: "or continue with"
    - Google OAuth button: white bg, border, Google logo placeholder, "Sign in with Google" — below button add annotation note: "→ shows 'Coming soon' toast on click (visual only)"
    - Footer link: "Already have an account? Login" linking to /login
    Add annotation note: "← NavShell omitted on auth pages — full-screen centered card layout per AUTH-01"

    **Frame 2: "login" — /login desktop**
    Same centered card layout as sign-up.
    Card contents:
    - LogoMark
    - Heading: "Welcome back" (Inter bold, brand-navy)
    - Form fields: Email (text input), Password (text input with show/hide toggle)
    - Row: "Remember me" checkbox (left) + "Forgot password?" link (right, brand-primary color)
    - "Sign In" CTA: brand-primary, full width
    - Divider + Google OAuth button (same "Coming soon" annotation)
    - Footer: "Don't have an account? Sign up"

    **Frame 3: "forgot-password" — /forgot-password desktop**
    Same centered card layout.
    Card contents:
    - LogoMark
    - Email envelope icon (brand-accent color, 48px)
    - Heading: "Check your email"
    - Body: "We've sent a password reset link to your email address."
    - "Back to Login" link (brand-primary)
    Add annotation: "Static confirmation screen — no API call; AUTH-03"

    **Frame 4: "sign-up--error" — /sign-up form error state**
    Copy of sign-up frame with error states applied:
    - Email field: red border (#EF4444), red inline error text below: "Please enter a valid email address"
    - Password field: red border, red inline error text: "Password must be at least 8 characters"
    - "Create Account" CTA: disabled state (opacity 50%, not-allowed cursor)
    - Add note: "Error state: inline field errors (red text below field) + disabled CTA — per CONTEXT.md annotation spec"

    **Frame 5: "login--error" — /login form error state**
    Copy of login frame with error applied:
    - Email or password field: red border + error text "Invalid email or password"
    - CTA disabled state
    - Note: "Error state variant per CONTEXT.md — shows inline validation before submit"

    After all 5 frames: call mcp__pencil__batch_get to confirm all 5 frame IDs exist (sign-up, login, forgot-password, sign-up--error, login--error).
  </action>
  <verify>
    <automated>mcp__pencil__batch_get on design/auth.pen — returns node list including frame IDs: sign-up, login, forgot-password, sign-up--error, login--error</automated>
  </verify>
  <done>5 frames exist in auth.pen. get_screenshot on "sign-up" shows centered card with brand-primary CTA, role selector, and Google OAuth button. get_screenshot on "sign-up--error" shows red field borders and disabled CTA. get_screenshot on "forgot-password" shows static confirmation card.</done>
</task>

</tasks>

<verification>
- mcp__pencil__batch_get on design/auth.pen returns 5 frame IDs: sign-up, login, forgot-password, sign-up--error, login--error
- mcp__pencil__get_screenshot on "sign-up" shows role selector (Candidate selected in brand-primary) and Google OAuth button with annotation
- mcp__pencil__get_screenshot on "sign-up--error" shows red field borders and disabled CTA button at 50% opacity
- mcp__pencil__get_variables confirms brand tokens are still set correctly after all operations
</verification>

<success_criteria>
DSGN-02 complete when: 3 base auth frames + 2 error state variants exist in auth.pen, role selector and OAuth button are visible in sign-up frame, and error state variants clearly show inline validation errors with disabled CTA.
</success_criteria>

<output>
After completion, create `.planning/phases/2.1-screen-design-inserted/2.1-02-SUMMARY.md`
</output>

---
phase: 2.1-screen-design-inserted
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - design/landing-pages.pen
  - design/auth.pen
  - design/test-flow.pen
  - design/dashboard.pen
  - design/payments.pen
  - design/components.pen
autonomous: false
requirements: [DSGN-01, DSGN-02, DSGN-03, DSGN-04, DSGN-05, DSGN-06]

must_haves:
  truths:
    - "The /design/ directory exists at project root"
    - "All 6 empty .pen files exist with valid minimal JSON structure"
    - "Pencil desktop app MCP connection is verified and responding"
  artifacts:
    - path: "design/landing-pages.pen"
      provides: "Empty .pen file ready for DSGN-01 design work"
    - path: "design/auth.pen"
      provides: "Empty .pen file ready for DSGN-02 design work"
    - path: "design/test-flow.pen"
      provides: "Empty .pen file ready for DSGN-03 design work"
    - path: "design/dashboard.pen"
      provides: "Empty .pen file ready for DSGN-04 design work"
    - path: "design/payments.pen"
      provides: "Empty .pen file ready for DSGN-05 design work"
    - path: "design/components.pen"
      provides: "Empty .pen file ready for DSGN-06 design work"
  key_links:
    - from: "design/*.pen files"
      to: "mcp__pencil__open_document"
      via: "file existence at expected path"
      pattern: "design/.*\\.pen"
---

<objective>
Create the /design/ directory and initialize 6 empty .pen files that subsequent design plans will populate. Verify the Pencil MCP connection is live before any design work begins.

Purpose: Pencil's CLI cannot create .pen files from scratch — files must pre-exist for open_document to operate on them. This Wave 0 unblocks all 6 design plans by ensuring their target files are present at the correct paths.
Output: /design/ directory + 6 valid empty .pen files + confirmed MCP connection.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/2.1-screen-design-inserted/2.1-CONTEXT.md
@.planning/phases/2.1-screen-design-inserted/2.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /design/ directory and initialize 6 empty .pen files</name>
  <files>design/landing-pages.pen, design/auth.pen, design/test-flow.pen, design/dashboard.pen, design/payments.pen, design/components.pen</files>
  <action>
    Use the Write tool to create the /design/ directory (by writing the first file into it) and then write all 6 .pen files with valid minimal JSON structure. Each file must be a well-formed .pen document that Pencil's open_document tool can open.

    The minimal valid .pen structure for each file:
    ```json
    {
      "version": "1.0",
      "variables": {},
      "nodes": []
    }
    ```

    Write each file at these exact paths from project root:
    - design/landing-pages.pen
    - design/auth.pen
    - design/test-flow.pen
    - design/dashboard.pen
    - design/payments.pen
    - design/components.pen

    Do NOT use Pencil MCP tools for this step — use the Write tool directly. This is intentional: Pencil CLI cannot create files, so Write tool bootstraps them.
  </action>
  <verify>
    <automated>ls design/ && cat design/landing-pages.pen</automated>
  </verify>
  <done>All 6 .pen files exist in /design/ with valid minimal JSON. `ls design/` shows all 6 filenames. `cat design/landing-pages.pen` shows valid JSON with version, variables, and nodes keys.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>6 empty .pen files in /design/ directory, each with minimal valid JSON structure ready for Pencil to open.</what-built>
  <how-to-verify>
    1. Open Pencil desktop application (must be running for all subsequent design plans)
    2. In Pencil, use File > Open and navigate to design/landing-pages.pen — confirm it opens without error
    3. Call mcp__pencil__get_editor_state — confirm it returns without a connection error and shows an active file
    4. If get_editor_state fails: Pencil MCP server is not running — check that Pencil is open and MCP integration is enabled in Pencil settings
    5. Once verified open, close the file in Pencil (other plans will open their respective files)
  </how-to-verify>
  <resume-signal>Type "verified" once Pencil is open and get_editor_state returns successfully, or describe the connection error if MCP is not responding</resume-signal>
</task>

</tasks>

<verification>
- /design/ directory exists at project root
- All 6 .pen files are present: `ls design/*.pen` returns 6 entries
- Each .pen file contains valid JSON (not empty): `cat design/auth.pen` shows JSON structure
- Pencil desktop app is open and MCP connection confirmed responsive
</verification>

<success_criteria>
Wave 0 complete when: all 6 .pen files exist at correct paths with valid minimal JSON, and Pencil desktop is open with a working MCP connection (get_editor_state returns without error).
</success_criteria>

<output>
After completion, create `.planning/phases/2.1-screen-design-inserted/2.1-00-SUMMARY.md`
</output>

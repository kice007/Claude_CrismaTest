---
phase: 2.1-screen-design-inserted
plan: "06"
type: execute
wave: 6
depends_on: ["2.1-00", "2.1-01", "2.1-02", "2.1-03", "2.1-04", "2.1-05"]
files_modified:
  - design/components.pen
autonomous: true
requirements: [DSGN-06]

must_haves:
  truths:
    - "components.pen contains all 6 component reference groups: toast variants, modal variants, skeleton states, empty state, 404 page, 500 page"
    - "3 toast variants exist: success, warning, error — each showing the slide-in state from the right"
    - "4 modal variants exist: invite, calendar, export, contact — each showing both Desktop Dialog and Mobile Drawer forms"
    - "Skeleton shimmer state is shown in 2 contexts: table row variant and card grid variant"
    - "The component reference sheet is designed as a summary of what was built, referencing actual source file paths"
  artifacts:
    - path: "design/components.pen"
      provides: "Shared component reference sheet covering all Phase 2 UI primitives"
      contains: "node groups: toasts, modals, skeletons, empty-state, page-404, page-500"
  key_links:
    - from: "components.pen all nodes"
      to: "src/components/ source files"
      via: "annotation notes on each component group listing the exact import path"
      pattern: "src/components/.*\\.tsx"
---

<objective>
Design the shared component reference sheet in components.pen — the final Phase 2.1 file. This is a summary document: all Phase 2 components documented with their variants, states, and source file paths in a single reference sheet for Phase 3–6 implementers.

Purpose: DSGN-06 — implementers need to see all reusable UI primitives (toast, modal, skeleton, empty state, 404, 500) in one place so they know what already exists and how to import it. This prevents Phase 3+ plans from reimplementing components that Phase 2 already built.
Output: design/components.pen with 6 component reference groups, each referencing the actual source file.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/2.1-screen-design-inserted/2.1-CONTEXT.md
@.planning/phases/2.1-screen-design-inserted/2.1-RESEARCH.md

<!-- Brand tokens (must be re-set in this file):
  brand-primary:   #1B4FD8
  brand-secondary: #3B6FE8
  brand-navy:      #0F2A6B
  brand-light:     #EEF2FF
  brand-accent:    #6366F1
  font-sans:       Inter
  font-mono:       JetBrains Mono
-->

<!-- Node naming convention for this file (groups, not page frames):
  "group-toasts"        — 3 toast variant frames side-by-side
  "group-modals"        — 4 modal variant frames (dialog desktop + drawer mobile)
  "group-skeletons"     — 2 skeleton context frames (table row, card grid)
  "group-empty-state"   — 1 EmptyState component frame
  "group-page-404"      — 1 branded 404 page frame
  "group-page-500"      — 1 branded 500 page frame
-->

<!-- Phase 2 components already built (confirmed from STATE.md decisions):
  src/components/ui/sonner.tsx (via shadcn) — Toast: success/warning/error, slide-in 250ms, 4s auto-dismiss
  src/components/modals/InviteModal.tsx — Dialog (desktop) / Drawer (mobile) for inviting candidate
  src/components/modals/CalendarModal.tsx — Dialog/Drawer for scheduling interview
  src/components/modals/ExportModal.tsx — Dialog/Drawer for exporting report
  src/components/modals/ContactModal.tsx — Dialog/Drawer for contacting candidate
  src/components/Skeleton.tsx — shimmer 1.5s infinite, composable by className
  src/components/EmptyState.tsx — illustrated card, blue-palette abstract shapes, body + CTA
  src/app/not-found.tsx — branded 404 page, no stack trace, link to home
  src/app/error.tsx — branded 500 page, no stack trace, link to home
-->

<!-- CONTEXT.md anti-pattern to honor:
  "Putting components.pen content in other files first: The component reference sheet is built last,
  as a summary. Do not build toast/modal variants from scratch in dashboard.pen — callout their
  existence as annotations instead."
  — This is now the last file, so we build the full variants here.
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap components.pen + design toast variants and modal variants</name>
  <files>design/components.pen</files>
  <action>
    1. Call mcp__pencil__open_document on "design/components.pen".
    2. Call mcp__pencil__set_variables with all 7 brand tokens.
    3. Call mcp__pencil__get_variables to confirm.

    This file is a reference sheet, not a page-sized frame. Use a large artboard layout with labeled groups arranged in a grid. Frame each group with a section header label.

    **Group 1: "group-toasts" — Toast variants (3 side-by-side)**
    Create a group frame labeled "Toast Variants" containing 3 toast component frames:

    Toast A — "toast-success": white bg, green left-border (4px, #22C55E), checkmark icon (green), title "Action completed" (Inter semibold), body "Your changes have been saved." — position: bottom-right corner of page simulation
    Toast B — "toast-warning": white bg, amber left-border (#F59E0B), warning icon, title "Heads up", body "This action cannot be undone."
    Toast C — "toast-error": white bg, red left-border (#EF4444), X icon, title "Error", body "Something went wrong. Please try again."

    Add annotation note on group: "← Toaster (Sonner) from src/app/layout.tsx — slide-in from right 250ms ease-out, 4s auto-dismiss; UI-03"

    Call mcp__pencil__get_screenshot on "group-toasts" and verify.

    **Group 2: "group-modals" — Modal variants (4 modals, desktop Dialog + mobile Drawer for each)**
    Create a group frame labeled "Modal Variants — Desktop Dialog / Mobile Drawer" with 2 rows × 4 cols:

    Row 1 (Desktop Dialog versions, 480px wide each):
    - Invite Modal: "Invite Candidate" header, email input, role dropdown, "Send Invite" button (brand-primary)
    - Calendar Modal: "Schedule Interview" header, date picker placeholder "[Date picker]", time dropdown, "Confirm" button
    - Export Modal: "Export Report" header, format selector (PDF/CSV), "Download" button
    - Contact Modal: "Contact Candidate" header, email prefilled (readonly), subject input, message textarea, "Send" button

    Row 2 (Mobile Drawer versions, 375px wide, bottom sheet style):
    - Same 4 modals but rendered as bottom-slide drawers (rounded top corners, drag handle bar at top)

    Add annotation note: "← All modals: Dialog (desktop) / Drawer (mobile) via Base UI; focus-trapped; ESC to close; UI-04. InviteModal: src/components/modals/InviteModal.tsx, CalendarModal: CalendarModal.tsx, ExportModal: ExportModal.tsx, ContactModal: ContactModal.tsx"

    Call mcp__pencil__get_screenshot on "group-modals" and verify.
  </action>
  <verify>
    <automated>mcp__pencil__batch_get on design/components.pen — returns node IDs: group-toasts, group-modals</automated>
  </verify>
  <done>2 groups confirmed. get_screenshot on "group-toasts" shows 3 toast variants side-by-side with correct color-coded left borders. get_screenshot on "group-modals" shows 4 modal types in both Dialog (desktop) and Drawer (mobile) forms.</done>
</task>

<task type="auto">
  <name>Task 2: Design skeleton states, empty state, 404 and 500 pages</name>
  <files>design/components.pen</files>
  <action>
    **Group 3: "group-skeletons" — Skeleton shimmer states (2 contexts)**
    Create a group frame labeled "Skeleton States" with 2 side-by-side frames:

    Skeleton A — "skeleton-table": shows a partial data table (4 rows) where all cells are replaced with shimmer rectangles. Shimmer rect: brand-light bg with gradient overlay labeled "[shimmer animation: 1.5s infinite horizontal gradient]". Column headers remain as text (not shimmer).
    Skeleton B — "skeleton-cards": shows a 2-col card grid (4 cards) where each card's content is shimmer rects (circle for avatar placeholder, 2 rects for text lines, 1 rect for badge area).

    Add annotation: "← Skeleton from src/components/Skeleton.tsx — composable via className prop; shimmer: 1.5s ease-in-out infinite; used on dashboard table and talent pool grid during fetch; UI-01"

    Call mcp__pencil__get_screenshot on "group-skeletons" and verify.

    **Group 4: "group-empty-state" — EmptyState component**
    Create a group frame labeled "Empty State" containing the EmptyState component frame:
    - Centered card (white bg, rounded-lg, shadow-sm)
    - Abstract blue-palette illustration area (placeholder "[Abstract blue shapes illustration]")
    - Heading: "No results found" (Inter semibold, brand-navy)
    - Body: "Try adjusting your filters or invite a new candidate." (Inter regular, brand-navy/70)
    - CTA button: "Clear Filters" (brand-primary, outline variant)

    Add annotation: "← EmptyState from src/components/EmptyState.tsx — illustration is inline SVG with hardcoded brand hex values; UI-02"

    Call mcp__pencil__get_screenshot on "group-empty-state" and verify.

    **Group 5: "group-page-404" — Branded 404 page**
    Create a group frame labeled "404 Page" showing the full-page layout (1440px):
    - NavShell at top (ref annotation: "← NavShell")
    - Centered content: large "404" text (Inter bold, brand-navy, 96px), heading "Page not found", body "The page you're looking for doesn't exist or has been moved.", "Go back home" button (brand-primary)
    - No stack trace, no technical error message

    Add annotation: "← src/app/not-found.tsx — Server Component; no stack trace; UI-05"

    Call mcp__pencil__get_screenshot on "group-page-404" and verify.

    **Group 6: "group-page-500" — Branded 500 page**
    Create a group frame labeled "500 Page" showing full-page layout (1440px):
    - NavShell at top
    - Centered content: large "500" text (Inter bold, brand-navy, 96px), heading "Something went wrong", body "An unexpected error occurred. Our team has been notified.", "Try again" button (brand-primary) + "Go back home" link

    Add annotation: "← src/app/error.tsx — requires 'use client' (Next.js error boundary); no stack trace exposed; UI-05"

    Call mcp__pencil__get_screenshot on "group-page-500" and verify.

    Final verification: call mcp__pencil__batch_get to confirm all 6 group node IDs exist: group-toasts, group-modals, group-skeletons, group-empty-state, group-page-404, group-page-500.
    Call mcp__pencil__snapshot_layout on "group-modals" (most complex group — 8 modal frames).
  </action>
  <verify>
    <automated>mcp__pencil__batch_get on design/components.pen — returns all 6 node group IDs: group-toasts, group-modals, group-skeletons, group-empty-state, group-page-404, group-page-500</automated>
  </verify>
  <done>6 component groups confirmed in components.pen. get_screenshot on "group-skeletons" shows shimmer rects in both table and card contexts. get_screenshot on "group-empty-state" shows illustration placeholder + branded heading + outline CTA. get_screenshot on "group-page-404" shows "404" large text with no stack trace. All groups have annotation notes with source file paths.</done>
</task>

</tasks>

<verification>
- mcp__pencil__batch_get on design/components.pen returns all 6 group node IDs
- mcp__pencil__get_screenshot on "group-toasts" shows 3 toast variants with green/amber/red left-border color coding
- mcp__pencil__get_screenshot on "group-modals" shows all 4 modal types in both Dialog and Drawer form (8 modal frames total in group)
- mcp__pencil__get_screenshot on "group-page-404" shows no stack trace or technical error text
- All 6 groups have annotation note nodes referencing the correct src/components/*.tsx source file paths
- mcp__pencil__snapshot_layout on "group-modals" reports no critical positioning errors
</verification>

<success_criteria>
DSGN-06 complete when: all 6 component groups exist in components.pen with correct visual variants, every group has an annotation note linking to the actual source file, and the file serves as a complete self-contained reference for all Phase 2 UI primitives.

Phase 2.1 as a whole is complete when all 6 .pen files exist in /design/ and all 6 DSGN requirements are fulfilled:
- design/landing-pages.pen — DSGN-01 — 6 frames
- design/auth.pen — DSGN-02 — 5 frames
- design/test-flow.pen — DSGN-03 — 12 frames
- design/dashboard.pen — DSGN-04 — 13 frames
- design/payments.pen — DSGN-05 — 2 frames
- design/components.pen — DSGN-06 — 6 groups
Total: 44 design nodes across 6 files.
</success_criteria>

<output>
After completion, create `.planning/phases/2.1-screen-design-inserted/2.1-06-SUMMARY.md`
</output>

---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/lib/motion.ts
autonomous: true
requirements:
  - DSYS-01
  - DSYS-02
  - DSYS-03

must_haves:
  truths:
    - "Tailwind utility classes bg-brand-primary, text-brand-navy, bg-brand-accent etc. resolve to the correct hex values"
    - "Shadcn/ui semantic CSS variables (--primary, --secondary, --accent) map to brand colors, not shadcn defaults"
    - "Custom border-radius tokens (--radius-card, --radius-chip, --radius-badge) and shadow tokens (--shadow-card, --shadow-dropdown, --shadow-modal) exist in @theme"
    - "All animation variants (fadeIn, slideUp, slideIn, staggerChildren, scaleIn) are exported from src/lib/motion.ts using the motion/react package"
    - "The dark mode @media block is removed from globals.css (light mode only in v1)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Brand token definitions, Shadcn CSS variable overrides, @theme inline block"
      contains: "--color-brand-primary"
    - path: "src/lib/motion.ts"
      provides: "Shared Framer Motion variants for all phases"
      exports: ["fadeIn", "slideUp", "slideIn", "staggerChildren", "scaleIn"]
  key_links:
    - from: "src/app/globals.css :root"
      to: "globals.css @theme inline"
      via: "var() references"
      pattern: "--color-primary: var\\(--primary\\)"
    - from: "src/lib/motion.ts"
      to: "motion/react"
      via: "import type { Variants }"
      pattern: "from \"motion/react\""
---

<objective>
Replace the Next.js scaffold CSS with CrismaTest brand tokens and initialize Shadcn/ui, then create the shared Framer Motion variants library.

Purpose: Every subsequent component must be able to use Tailwind utilities like `bg-brand-primary` and `text-brand-navy` and import animation variants from one place. This plan establishes both foundations before any UI is built.

Output:
- `src/app/globals.css` — complete brand token system with Shadcn CSS variable overrides
- `src/lib/motion.ts` — shared animation variants library
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Existing globals.css — full replacement target -->
<!-- Current state (scaffold defaults to remove/replace): -->
/*
@import "tailwindcss";
:root { --background: #ffffff; --foreground: #171717; }
@theme inline { --color-background: var(--background); --color-foreground: var(--foreground); --font-sans: var(--font-geist-sans); --font-mono: var(--font-geist-mono); }
@media (prefers-color-scheme: dark) { :root { --background: #0a0a0a; --foreground: #ededed; } }
body { background: var(--background); color: var(--foreground); font-family: Arial, Helvetica, sans-serif; }
*/

<!-- Plan 02 will add --font-inter and --font-jetbrains-mono CSS vars via next/font in layout.tsx -->
<!-- This plan's @theme inline block must reference var(--font-inter) and var(--font-jetbrains-mono) -->
<!-- Those vars won't be present until Plan 02 runs, but the CSS reference is safe to add now -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shadcn/ui init and brand token system in globals.css</name>
  <files>src/app/globals.css</files>
  <action>
    Step 1 — Run Shadcn init:
    ```
    npx shadcn@latest init
    ```
    When prompted: accept defaults, choose CSS variables: yes, globals.css path: src/app/globals.css. This will overwrite globals.css with shadcn defaults.

    Step 2 — After init completes, completely replace the content of globals.css with the full brand token system below. Do NOT keep shadcn's generated color values — replace them entirely with brand-mapped HSL equivalents.

    The final globals.css must contain in this order:

    1. `@import "tailwindcss";`

    2. `:root` block with:
       - Brand raw values as hex CSS vars: `--brand-primary-raw: #1B4FD8`, `--brand-secondary-raw: #3B6FE8`, `--brand-navy-raw: #0F2A6B`, `--brand-light-raw: #EEF2FF`, `--brand-accent-raw: #6366F1`
       - Shadcn semantic vars mapped to brand HSL equivalents:
         - `--background: hsl(0 0% 100%)`
         - `--foreground: hsl(222 47% 11%)`
         - `--card: hsl(0 0% 100%)`
         - `--card-foreground: hsl(222 47% 11%)`
         - `--popover: hsl(0 0% 100%)`
         - `--popover-foreground: hsl(222 47% 11%)`
         - `--primary: hsl(221 77% 47%)` (brand-primary #1B4FD8)
         - `--primary-foreground: hsl(0 0% 100%)`
         - `--secondary: hsl(221 77% 57%)` (brand-secondary #3B6FE8)
         - `--secondary-foreground: hsl(0 0% 100%)`
         - `--muted: hsl(221 30% 96%)`
         - `--muted-foreground: hsl(222 20% 45%)`
         - `--accent: hsl(239 84% 67%)` (brand-accent #6366F1)
         - `--accent-foreground: hsl(0 0% 100%)`
         - `--destructive: hsl(0 84% 60%)`
         - `--destructive-foreground: hsl(0 0% 100%)`
         - `--border: hsl(221 20% 88%)`
         - `--input: hsl(221 20% 88%)`
         - `--ring: hsl(221 77% 47%)`
         - `--radius: 0.5rem`
       - No dark mode block — remove it entirely (v1 is light mode only per requirements)

    3. `@theme inline` block with ALL of the following:
       - Font vars: `--font-sans: var(--font-inter)` and `--font-mono: var(--font-jetbrains-mono)` (these CSS vars come from Plan 02's next/font setup; referencing them here is safe — they'll resolve once Plan 02 runs)
       - Brand color utilities:
         - `--color-brand-primary: #1B4FD8`
         - `--color-brand-secondary: #3B6FE8`
         - `--color-brand-navy: #0F2A6B`
         - `--color-brand-light: #EEF2FF`
         - `--color-brand-accent: #6366F1`
       - Neutral scale: `--color-neutral-50: #f8fafc`, `--color-neutral-100: #f1f5f9`, `--color-neutral-200: #e2e8f0`, `--color-neutral-500: #64748b`, `--color-neutral-700: #334155`, `--color-neutral-900: #0f172a`
       - Status colors: `--color-success: #16a34a`, `--color-warning: #d97706`, `--color-danger: #dc2626`
       - Shadcn semantic → Tailwind utilities (using `@theme inline` so var() resolves at runtime):
         - `--color-background: var(--background)`
         - `--color-foreground: var(--foreground)`
         - `--color-card: var(--card)`
         - `--color-card-foreground: var(--card-foreground)`
         - `--color-popover: var(--popover)`
         - `--color-popover-foreground: var(--popover-foreground)`
         - `--color-primary: var(--primary)`
         - `--color-primary-foreground: var(--primary-foreground)`
         - `--color-secondary: var(--secondary)`
         - `--color-secondary-foreground: var(--secondary-foreground)`
         - `--color-muted: var(--muted)`
         - `--color-muted-foreground: var(--muted-foreground)`
         - `--color-accent: var(--accent)`
         - `--color-accent-foreground: var(--accent-foreground)`
         - `--color-destructive: var(--destructive)`
         - `--color-destructive-foreground: var(--destructive-foreground)`
         - `--color-border: var(--border)`
         - `--color-input: var(--input)`
         - `--color-ring: var(--ring)`
       - Custom border-radius tokens:
         - `--radius-card: 0.75rem`
         - `--radius-chip: 9999px`
         - `--radius-badge: 0.375rem`
       - Custom shadow tokens (brand-navy based for premium SaaS depth):
         - `--shadow-card: 0 1px 3px 0 rgb(15 42 107 / 0.08), 0 4px 16px -2px rgb(15 42 107 / 0.06)`
         - `--shadow-dropdown: 0 4px 16px -2px rgb(15 42 107 / 0.12), 0 1px 4px 0 rgb(15 42 107 / 0.06)`
         - `--shadow-modal: 0 20px 48px -8px rgb(15 42 107 / 0.2), 0 4px 16px -4px rgb(15 42 107 / 0.1)`

    4. `body` base styles:
       - `background: var(--background)`
       - `color: var(--foreground)`
       - `font-family: var(--font-sans, Arial, Helvetica, sans-serif)`

    CRITICAL: Use `@theme inline` (not bare `@theme`) for all blocks that reference `var()`. Bare `@theme` is only used for literal values.
    CRITICAL: Remove the `@media (prefers-color-scheme: dark)` block entirely.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `npm run build` exits 0 with no TypeScript or CSS errors
    - globals.css contains `--color-brand-primary: #1B4FD8` in @theme inline
    - globals.css contains `--primary: hsl(221 77% 47%)` in :root
    - globals.css contains `--radius-card: 0.75rem` in @theme inline
    - globals.css contains `--shadow-modal` in @theme inline
    - No dark mode @media block present
  </done>
</task>

<task type="auto">
  <name>Task 2: Shared Framer Motion variants library</name>
  <files>src/lib/motion.ts</files>
  <action>
    Install the motion package (renamed from framer-motion):
    ```
    npm install motion
    ```

    Create `src/lib/motion.ts` (create `src/lib/` directory if it doesn't exist):

    ```typescript
    // src/lib/motion.ts
    // Shared animation variants — import from here in all phases.
    // MotionConfig reducedMotion="user" in layout.tsx handles prefers-reduced-motion globally.
    import type { Variants } from "motion/react";

    export const fadeIn: Variants = {
      hidden: { opacity: 0 },
      visible: {
        opacity: 1,
        transition: { duration: 0.4, ease: "easeOut" },
      },
    };

    export const slideUp: Variants = {
      hidden: { opacity: 0, y: 20 },
      visible: {
        opacity: 1,
        y: 0,
        transition: { duration: 0.5, ease: [0.22, 1, 0.36, 1] },
      },
    };

    export const slideIn: Variants = {
      hidden: { opacity: 0, x: -20 },
      visible: {
        opacity: 1,
        x: 0,
        transition: { duration: 0.4, ease: "easeOut" },
      },
    };

    export const scaleIn: Variants = {
      hidden: { opacity: 0, scale: 0.95 },
      visible: {
        opacity: 1,
        scale: 1,
        transition: { duration: 0.3, ease: "easeOut" },
      },
    };

    export const staggerChildren: Variants = {
      hidden: {},
      visible: {
        transition: { staggerChildren: 0.08, delayChildren: 0.1 },
      },
    };
    ```

    Do NOT use `useMemo` or `useCallback` anywhere in this file — React Compiler is active (see CLAUDE.md).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `npm run build` exits 0
    - `src/lib/motion.ts` exports fadeIn, slideUp, slideIn, scaleIn, staggerChildren
    - Imports from "motion/react" (not "framer-motion")
    - TypeScript compilation produces no errors for this file
  </done>
</task>

</tasks>

<verification>
Run after both tasks complete:
1. `npm run lint` — zero ESLint errors
2. `npm run build` — zero TypeScript errors, build completes successfully
3. Check globals.css manually: confirm `--color-brand-primary: #1B4FD8` appears in @theme inline, `--primary: hsl(221 77% 47%)` appears in :root, no dark mode @media block
4. Check src/lib/motion.ts: confirm all 5 variants exported, imports from "motion/react"
</verification>

<success_criteria>
- `npm run build` exits 0 with no errors
- All brand color tokens defined in globals.css @theme inline (bg-brand-primary etc. available as Tailwind utilities)
- Shadcn semantic vars in :root use brand-mapped HSL values
- Custom radius and shadow tokens exist in @theme inline
- src/lib/motion.ts exports all 5 animation variants from motion/react
- No dark mode CSS block in globals.css
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` using the summary template.
</output>

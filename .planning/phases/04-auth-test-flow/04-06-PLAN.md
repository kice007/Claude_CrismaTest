---
phase: 04-auth-test-flow
plan: "06"
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05"]
files_modified: []
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, TEST-01, TEST-02, TEST-03, TEST-04, TEST-05]

must_haves:
  truths:
    - "The full auth flow (sign-up → OTP → onboarding → dashboard) completes without errors"
    - "The login flow sets auth cookie and localStorage, nav shows avatar dropdown"
    - "The forgot-password 3-step flow completes to /login"
    - "Navigating to /dashboard while logged out redirects to /login (302 in Network tab)"
    - "The complete test flow (intro → user-info → check → questions → calculating → result) navigates end-to-end"
    - "The result page gauge animates and confetti fires"
    - "npm run build exits 0 with no type errors and no lint warnings"
  artifacts:
    - path: "src/proxy.ts"
      provides: "Verified: /dashboard and /test/* redirect when logged out"
    - path: "src/app/(test)/test/[id]/result/page.tsx"
      provides: "Verified: confetti fires for score > 70"
  key_links:
    - from: "human-verify"
      to: "all Phase 4 requirements"
      via: "manual browser walkthrough"
      pattern: "n/a"
---

<objective>
Human verification checkpoint: confirm both complete user flows work end-to-end in the browser. The automated build must pass first; then the user manually walks through auth and test flows.

Purpose: AUTH-01–05 and TEST-01–05 are all manual-only requirements (browser APIs, visual flows, redirect behavior). This checkpoint is the only way to verify them per VALIDATION.md.
Output: User approval that Phase 4 is complete.
</objective>

<execution_context>
@C:/Users/chris/Downloads/Projects/CrismaTech/CrismTest/crismatest/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/Downloads/Projects/CrismaTech/CrismTest/crismatest/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-auth-test-flow/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final automated checks (build + lint)</name>
  <files></files>
  <action>
Run the full automated validation suite before the human checkpoint:

```bash
npm run build
npm run lint
npm run type-check
```

All three must exit 0. Fix any remaining TypeScript errors or lint warnings before proceeding to the human verification task.

Common issues to check:
- `motion/react` Variants import: must use `import type { Variants } from 'motion-dom'` (established Phase 01 decision)
- `standardSchemaResolver` import: must be from `@hookform/resolvers` (not `@hookform/resolvers/zod`)
- `useParams` returns `string | string[]` in Next.js — use `useParams<{ id: string }>()` generic for type safety
- sessionStorage access: must be in `useEffect` or event handlers — never at module/component top level
- `proxy.ts` function name must be `proxy`, not `middleware`
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run type-check</automated>
  </verify>
  <done>
    All three commands exit 0 — no build errors, no lint warnings, no TypeScript errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — auth + test flows end-to-end</name>
  <what-built>
    Phase 4 Auth + Test Flow — all AUTH (01–05) and TEST (01–05) requirements implemented.

    **Auth flow:**
    - /sign-up: 5-step (form → OTP → onboarding-1 → onboarding-2 → all-set → /dashboard)
    - /login: email + password → /dashboard
    - /forgot-password: 3-step (email → OTP → reset → /login)
    - NavShell: avatar+dropdown when logged in, Login/Sign Up when logged out
    - Protected routes: proxy.ts redirects /dashboard and /test/* when logged out

    **Test flow:**
    - /test/sample/intro: split-panel, role info, Start Test →
    - /test/sample/user-info: 5-field form, stores to sessionStorage
    - /test/sample/check: webcam preview, mic bars, checklist, I'm Ready gate
    - /test/sample/questions: 12-question carousel, 6 formats, countdown timer
    - /test/sample/calculating: stagger animation, 6s auto-redirect
    - /test/sample/result: animated gauge, confetti (score > 70), share/retake CTAs
  </what-built>
  <how-to-verify>
    **Start the dev server:** `npm run dev`

    **Auth Flow — Test in order:**
    1. Visit http://localhost:3000 — confirm Login and Sign Up buttons visible in nav
    2. Visit http://localhost:3000/sign-up
       - Fill all 7 fields, submit → OTP step appears (no page reload)
       - Enter any 6 digits → onboarding step 1 appears
       - Fill industry + hiring volume, click Continue → step 2 appears
       - Select 1+ role chips, click Finish → All Set modal appears
       - Click "Go to Dashboard" → redirects to /dashboard
       - Confirm nav now shows avatar (JD circle) + "John Doe"
    3. Click the avatar → dropdown shows My Profile, Settings, Log out
    4. Click Log out → redirects to / → nav shows Login/Sign Up again
    5. Visit http://localhost:3000/login
       - Fill any email + password, click Log In → redirects to /dashboard
       - Nav shows avatar dropdown
    6. While logged in, click the JD avatar → Log out → redirects to /
    7. **Protected route test:** Clear all cookies in DevTools (Application → Cookies → Clear All)
       Then navigate to http://localhost:3000/dashboard
       Confirm: browser redirects to /login (check Network tab for 302)
    8. Visit http://localhost:3000/forgot-password
       - Enter any email, click Send Reset Code → /forgot-password/verify
       - Enter any 6 digits → /forgot-password/reset
       - Enter matching passwords (8+ chars) → redirects to /login
    9. Visit /login and log in again

    **Test Flow — Test in order:**
    10. While logged in, visit http://localhost:3000/test/sample/intro
        - Confirm split-panel: dark navy left, white right
        - Confirm role "Customer Support Specialist", "15 minutes", 4 modules visible
        - Webcam disclaimer chip visible
        - Click "Start Test →" → /test/sample/user-info
    11. On user-info page:
        - Confirm split-panel, "Step 1 of 2" badge, 5 fields
        - Fill all fields, click "Continue →"
        - Check DevTools → Application → Session Storage → `crismatest_candidate_info` key exists
        - → /test/sample/check
    12. On check page:
        - Click "Check Permissions" → browser prompts for camera + mic access
        - Allow permissions → webcam feed appears, mic bars animate, camera+mic boxes auto-check
        - Check "Alone in room" and "No extra screens" checkboxes
        - Check the disclaimer checkbox
        - Confirm "I'm Ready" button is now enabled (not grayed out)
        - Click "I'm Ready" → /test/sample/questions
    13. On questions page:
        - Confirm progress bar, module badge, and countdown timer (MM:SS in monospace)
        - Navigate through questions to find a QCM (q1) — click an option, confirm selection highlight
        - Navigate to a drag-and-drop question (q2) — drag an item to a different position
        - Navigate to a case study (q3) — confirm scenario card and 3 textarea fields
        - Navigate to simulation (q4) — confirm two-column layout and selectable options
        - Navigate to audio/video (q5) — confirm dark video area and record button
        - Navigate to short text (q6) — type text, confirm word counter updates
        - Wait until timer shows 1:00 → confirm timer turns amber (#F59E0B)
        - Wait until timer shows 0:30 → confirm timer turns red (#EF4444) [Optional — can skip if time-consuming, use fast refresh to test]
        - Navigate to last question (q12) → confirm "Submit Test" button in footer
        - Click "Submit Test" → /test/sample/calculating
    14. On calculating page:
        - Confirm dark navy background with centered card
        - 4 step labels fade in sequentially
        - After ~6 seconds, auto-redirects to /test/sample/result
    15. On result page:
        - Confirm gauge animates from 0 to final score
        - With 12 questions answered: score = min(95, 60 + 36) = 95 (or close) — check actual value
        - Since score > 70: confetti fires AND congratulations banner shown
        - 5 sub-score bars animate in
        - "Copy Link" → toast notification appears
        - "Improve Your Score" → /pricing link
        - "Retake Test" → /test/sample/intro link

    **Final checks:**
    16. Run `npm run build` one more time → confirm 0 errors
    17. Check browser console — no unhandled errors or warnings on any page visited
  </how-to-verify>
  <resume-signal>
    Type "approved" if all flows work correctly.
    Or describe any issues found (include which step number failed and what you observed).
  </resume-signal>
</task>

</tasks>

<verification>
Phase 4 is complete when:
- `npm run build && npm run lint && npm run type-check` all exit 0
- Human verification: both complete flows tested and approved
- All AUTH-01 through AUTH-05 and TEST-01 through TEST-05 behaviors confirmed in browser
</verification>

<success_criteria>
- Automated: build, lint, type-check all green
- Manual: auth flow (sign-up, login, forgot-password, nav avatar, protected routes) fully verified
- Manual: test flow (intro, user-info, check, questions, calculating, result) fully verified
- Confetti fires on result page (score > 70)
- Protected routes redirect to /login when logged out
- Phase 4 approved by human
</success_criteria>

<output>
After completion, create `.planning/phases/04-auth-test-flow/04-06-SUMMARY.md`
</output>

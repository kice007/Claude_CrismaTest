---
phase: 03-landing-pages-data-foundation
plan: "03"
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - src/app/page.tsx
  - src/components/home/HeroSection.tsx
  - src/components/home/TrustBar.tsx
  - src/components/home/WhatIsSection.tsx
  - src/components/home/HowItWorksSection.tsx
  - src/components/home/ForCompaniesSection.tsx
  - src/components/home/AntifraudSection.tsx
  - src/components/home/TestLibrarySection.tsx
  - src/components/home/TestimonialsSection.tsx
  - src/components/home/FaqSection.tsx
  - src/components/home/ContactSection.tsx
  - src/components/home/ContactForm.tsx
  - src/components/home/FooterSection.tsx
autonomous: true
requirements: [LAND-01]

must_haves:
  truths:
    - "Visiting / renders all 11 landing page sections with correct content"
    - "All text content comes from useTranslation() — no hardcoded English strings in JSX"
    - "Page renders without horizontal overflow at 320px viewport"
    - "All section components accept variant prop (light | dark) for Plan 04 reuse"
    - "Contact form renders with all 6 fields, GDPR checkbox, and submit button"
    - "Hero section shows navy background with floating dashboard card mockup"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Home-light page route"
    - path: "src/components/home/HeroSection.tsx"
      provides: "Hero with floating card + dual CTAs"
    - path: "src/components/home/ContactForm.tsx"
      provides: "RHF + zod contact form (client component)"
    - path: "src/components/home/FooterSection.tsx"
      provides: "Full footer with nav links + social"
  key_links:
    - from: "src/components/home/*.tsx"
      to: "locales/en.json"
      via: "useTranslation() t() calls"
      pattern: "t\\(\"home_"
    - from: "src/components/home/ContactForm.tsx"
      to: "src/app/api/contact/route.ts"
      via: "fetch POST /api/contact"
      pattern: "fetch.*api/contact"
    - from: "src/app/page.tsx"
      to: "src/components/home/*.tsx"
      via: "section imports"
      pattern: "import.*from.*@/components/home"
---

<objective>
Build the home-light page (/) — all 11 section components + the root page.tsx assembly. This is the main deliverable for LAND-01. Section components accept a `variant` prop so the dark page (Plan 04) can reuse them without duplication.

Purpose: The primary marketing surface. Companies and candidates first land here. All sections specified in CONTEXT.md must be present.
Output: Fully functional, bilingual, responsive home-light page at /.
</objective>

<execution_context>
@C:/Users/chris/Downloads/Projects/CrismaTech/CrismTest/crismatest/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/Downloads/Projects/CrismaTech/CrismTest/crismatest/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-landing-pages-data-foundation/03-CONTEXT.md
@.planning/phases/03-landing-pages-data-foundation/03-RESEARCH.md
@src/lib/motion.ts
@locales/en.json
</context>

<interfaces>
<!-- Reusable assets already in codebase — use these directly -->

From src/lib/motion.ts:
```typescript
export const fadeIn: Variants  // opacity 0→1, y 20→0, 0.4s ease-out
export const slideUp: Variants // y 40→0, 0.5s ease-out
export const staggerChildren: Variants // staggerChildren: 0.1
```

From src/components/Skeleton.tsx:
```typescript
export function Skeleton({ className }: { className?: string }): JSX.Element
// Use for placeholder shimmer in any section while data loads
```

From src/components/ui/sonner.tsx (Toaster already wired in layout.tsx):
```typescript
import { toast } from 'sonner'
toast.success(message)
toast.error(message)
```

Existing i18n pattern:
```typescript
"use client";
import { useTranslation } from "react-i18next";
export function SomeSection({ variant = "light" }: { variant?: "light" | "dark" }) {
  const { t } = useTranslation();
  return <section>{t("home_hero_headline")}</section>
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Section components (HeroSection through TestimonialsSection)</name>
  <files>
    src/components/home/HeroSection.tsx,
    src/components/home/TrustBar.tsx,
    src/components/home/WhatIsSection.tsx,
    src/components/home/HowItWorksSection.tsx,
    src/components/home/ForCompaniesSection.tsx,
    src/components/home/AntifraudSection.tsx,
    src/components/home/TestLibrarySection.tsx,
    src/components/home/TestimonialsSection.tsx
  </files>
  <action>
    Create the `src/components/home/` directory and build these 8 section components. All components:
    - Must have `"use client"` directive at top (required for useTranslation)
    - Accept `variant?: "light" | "dark"` prop (default "light")
    - Use `useTranslation()` for ALL text — no hardcoded strings in JSX
    - Import animations from `src/lib/motion.ts` (not defined inline)
    - Use Tailwind v4 brand tokens: `bg-brand-navy`, `text-brand-primary`, `text-brand-navy`, `bg-brand-light`, etc.
    - Responsive: mobile-first, 320px min-width safe
    - Touch targets: 48px min height on all interactive elements

    **Variant color logic** (apply conditionally via `variant` prop):
    - light: white/gray backgrounds, dark text
    - dark: brand-navy backgrounds, white/light text
    - Use conditional Tailwind classes: `variant === "dark" ? "bg-brand-navy text-white" : "bg-white text-brand-navy"`

    **HeroSection.tsx:**
    - Full-width section, `bg-brand-navy` (navy even on light page — hero is always dark per design spec)
    - Centered text: `home_hero_headline` (large, white), `home_hero_subline` (white/80 opacity)
    - Dual CTAs: "Take the Test" (brand-primary button) + "See How It Works" (ghost/outline white button)
    - Trust badge: small text using `home_hero_badge`
    - **Floating dashboard card mockup**: a frosted-glass card (backdrop-blur-sm, semi-transparent white background, rounded-2xl, shadow-2xl) positioned below the CTAs or floating right (desktop). The card shows a simplified candidate table mockup: 4–5 rows, each row has an avatar circle (initials, role-colored bg), a name, a role label, and a CrismaScore chip (colored by score range: green 75+, amber 60–74, red <60). Use hardcoded mock data for the card (NOT t() — this is a visual mockup, not content). Blurred/faded edges via CSS mask or `[mask-image:radial-gradient(ellipse_80%_50%_at_50%_0%,black_70%,transparent_100%)]`
    - Use `motion.div` with `fadeIn` variant for text reveal
    - Min height: 80vh on desktop

    **TrustBar.tsx:**
    - Horizontal marquee of 6 company name text items
    - Use `home_trust_label` as section eyebrow
    - Marquee: CSS animation (`animate-marquee` or inline keyframes via Tailwind arbitrary values) — duplicate the list twice for seamless loop
    - Light: gray-100 background. Dark: brand-navy/20 tint.

    **WhatIsSection.tsx:**
    - eyebrow (`home_what_is_eyebrow`), headline, body text
    - 3-stat row: 3 cards each showing value + label from `home_what_is_stat_*` keys
    - Light: white bg. Dark: darker navy variant.
    - Animate with `staggerChildren` + `fadeIn` on scroll (use `motion.div` with `whileInView`, `once: true`)

    **HowItWorksSection.tsx:**
    - eyebrow + headline
    - 3-step layout: numbered steps (01/02/03) with title + description
    - Desktop: horizontal row. Mobile: vertical stack.
    - Step numbers in brand-primary color, large font

    **ForCompaniesSection.tsx:**
    - eyebrow + headline
    - 6-feature grid: 3 cols on desktop, 2 on tablet, 1 on mobile
    - Each card: lucide-react icon (choose appropriate), title, desc
    - Icons: use `CheckCircle`, `Brain`, `Shield`, `Globe`, `Users`, `LayoutGrid` or similar from lucide-react
    - CTA button at bottom: `home_companies_cta`

    **AntifraudSection.tsx:**
    - eyebrow + headline + body
    - 4 features as a 2×2 grid (or vertical list on mobile)
    - Visual: dark section (bg-brand-navy always, regardless of variant) with brand-accent color accents
    - Each feature: checkmark icon + feature label

    **TestLibrarySection.tsx:**
    - eyebrow + headline + subtitle
    - 8 template cards in a grid (4 cols desktop, 2 tablet, 1 mobile)
    - Each card: role name, duration from a hardcoded map (vary 10–15 min), `home_tests_duration_label` suffix
    - CTA: `home_tests_cta`

    **TestimonialsSection.tsx:**
    - eyebrow + headline
    - Desktop: 3-column grid of testimonial cards
    - Mobile: horizontal scroll carousel (single card visible, swipe via `overflow-x-auto snap-x snap-mandatory`)
    - Each card: quote text, avatar circle (initials from name), name, role, company
    - Use `staggerChildren` + `slideUp` on desktop grid
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - All 8 component files exist in src/components/home/
    - npm run build succeeds (no TypeScript errors, no missing module errors)
    - All text uses t() calls — no hardcoded English strings in JSX
    - variant prop accepted by all components
  </done>
</task>

<task type="auto">
  <name>Task 2: FAQ, ContactSection, ContactForm, Footer + page assembly</name>
  <files>
    src/components/home/FaqSection.tsx,
    src/components/home/ContactSection.tsx,
    src/components/home/ContactForm.tsx,
    src/components/home/FooterSection.tsx,
    src/app/page.tsx
  </files>
  <action>
    **FaqSection.tsx:**
    - "use client" (for accordion open/close state)
    - eyebrow + headline
    - 7 FAQ items rendered as an accordion — use a simple `useState` array tracking open items
    - Each item: `home_faq_q{n}` question + `home_faq_a{n}` answer, chevron icon rotates on open
    - Do NOT use Shadcn Accordion component — simple custom accordion to avoid additional bundle weight
    - Smooth height transition: use `max-h-0 overflow-hidden transition-all duration-200` → `max-h-96` when open

    **ContactSection.tsx:**
    - "use client"
    - Wrapper section for the embedded contact form
    - Contains `home_trust_label` area at top and wraps `ContactForm`
    - Light: light-gray background. Dark: slightly lighter navy than hero.
    - Full-width section with max-w-2xl centered form

    **ContactForm.tsx** — the form component (core of contact functionality):
    - "use client"
    - Import: `useForm` from `react-hook-form`, `zodResolver` from `@hookform/resolvers/zod`, `z` from `zod`, `toast` from `sonner`
    - Zod schema (client-side mirror of API schema):
      ```typescript
      const formSchema = z.object({
        name: z.string().min(1, t("contact_form_error_required")).max(100),
        company: z.string().min(1, t("contact_form_error_required")).max(100),
        email: z.string().email(t("contact_form_error_email")),
        teamSize: z.enum(['1-10', '11-50', '51-200', '200+'], { required_error: t("contact_form_error_required") }),
        message: z.string().max(2000).optional(),
        gdprConsent: z.literal(true, { errorMap: () => ({ message: t("contact_form_error_required") }) }),
      })
      ```
    - Fields: name, company, email, teamSize (select), message (textarea), gdprConsent (checkbox)
    - All fields use `register()` from RHF
    - Inline errors: `{errors.fieldName && <p className="text-red-500 text-sm mt-1">{errors.fieldName.message}</p>}`
    - Submit handler: POST to `/api/contact` with `fetch`, set `isSubmitting` state during request
    - Success: `toast.success(t("contact_form_success_toast"))`, `reset()` the form
    - Rate limit (429): `toast.error(t("contact_form_error_rate_limit"))`
    - Other error: `toast.error(t("contact_form_error_generic"))`
    - Submit button: disabled when `!isValid || isSubmitting`. Shows `contact_form_submitting` text when loading.
    - GDPR checkbox: uses `watch('gdprConsent')` to control submit button enabled state
    - All inputs: 48px min height, 16px font size on mobile (prevents iOS zoom)
    - Install `@hookform/resolvers` if not in package.json: `npm install @hookform/resolvers`

    **FooterSection.tsx:**
    - "use client" (for useTranslation)
    - 3-column layout desktop, stacked mobile
    - Logo + tagline in first column
    - Product links column, Company links column
    - Copyright bar at bottom
    - Social icons (LinkedIn, Twitter) using lucide-react
    - Links to /for-candidates, /for-companies, /pricing, /about, /privacy, /terms — all use Next.js `<Link>` (these routes may not exist yet — that's OK)
    - Light: dark-gray bg (#111827 or brand-navy). Dark: deeper navy.

    **src/app/page.tsx** — assemble the home-light page:
    - Replace the current scaffold content entirely
    - Import all 11 section components from `@/components/home/`
    - No `variant` prop passed — defaults to "light"
    - Order: HeroSection, TrustBar, WhatIsSection, HowItWorksSection, ForCompaniesSection, AntifraudSection, TestLibrarySection, TestimonialsSection, FaqSection, ContactSection, FooterSection
    - Page is a Server Component (no "use client" needed on page.tsx itself — sections handle their own client needs)
    - Add `export const metadata` with title "CrismaTest — Hire the Best. Objectively." and description from hero subline
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>
    - All 4 files exist and export valid React components
    - npm run build succeeds with no errors
    - src/app/page.tsx assembles all 11 sections
    - ContactForm uses react-hook-form + zod with inline validation errors
    - No hardcoded English strings in any component
  </done>
</task>

</tasks>

<verification>
- `npm run build` — builds cleanly
- `npm run lint` — no errors
- Visit `/` in browser — all 11 sections render, EN/FR toggle changes all text, no horizontal overflow at 320px
- Hero section shows navy background with floating dashboard card mockup
- Contact form has all 6 fields + GDPR checkbox
</verification>

<success_criteria>
Home-light page renders all 11 sections. All content is bilingual via useTranslation(). Page passes build + lint. Contact form has correct fields and posts to /api/contact. Page renders without overflow at 320px. Hero shows the floating dashboard card.
</success_criteria>

<output>
After completion, create `.planning/phases/03-landing-pages-data-foundation/03-03-SUMMARY.md`
</output>

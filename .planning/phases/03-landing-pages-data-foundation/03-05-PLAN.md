---
phase: 03-landing-pages-data-foundation
plan: "05"
type: execute
wave: 4
depends_on: ["03-03", "03-04"]
files_modified: []
autonomous: false
requirements: [LAND-01, LAND-02, DATA-01, DATA-02, DATA-03, DATA-10, SEC-01]

must_haves:
  truths:
    - "/ and /dark both render without errors in the browser"
    - "Switching EN↔FR on both pages changes all visible text with no missing keys"
    - "No horizontal overflow at 320px viewport on any section of either page"
    - "Contact form submission saves data and triggers Resend email notification"
    - "4th contact form submission within 1 hour triggers rate-limit error toast (no stack trace)"
    - "npm run db:seed exits 0 with correct row counts"
    - ".env is not in git history"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Live home-light page"
    - path: "src/app/dark/page.tsx"
      provides: "Live home-dark page"
    - path: "src/app/api/contact/route.ts"
      provides: "Contact API endpoint"
  key_links:
    - from: "ContactForm"
      to: "Supabase contact_submissions"
      via: "POST /api/contact → createServerSupabaseClient"
      pattern: "contact_submissions"
    - from: "ContactForm"
      to: "Resend email"
      via: "POST /api/contact → resend.emails.send"
      pattern: "ADMIN_EMAIL"
---

<objective>
Human verification checkpoint. All code from Plans 01–04 is complete — this plan pauses for manual review of both landing pages and the contact form end-to-end flow. No new code is written here.

Purpose: Catch visual/functional issues that `npm run build` cannot detect: layout breaks at mobile viewports, missing translations, contact form UX, dark page color accuracy.
Output: Phase 3 confirmed complete or issues surfaced for gap closure.
</objective>

<execution_context>
@C:/Users/chris/Downloads/Projects/CrismaTech/CrismTest/crismatest/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/Downloads/Projects/CrismaTech/CrismTest/crismatest/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-landing-pages-data-foundation/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-verification automated checks</name>
  <files></files>
  <action>
    Run the full automated suite before asking for human review. This ensures there are no regressions before the human invests time in manual testing.

    Run in order:
    1. `npm run lint` — must pass with zero errors
    2. `npm run build` — must complete with zero errors
    3. `git log --all --full-history -- .env` — must return NO commits (SEC-01)
    4. `node -e "const en = require('./locales/en.json'); const fr = require('./locales/fr.json'); const missing = Object.keys(en).filter(k => k.startsWith('home_') || k.startsWith('contact_form_')).filter(k => !fr[k]); console.log(missing.length === 0 ? 'PASS: all i18n keys present' : 'FAIL: missing ' + missing.join(', '))"` — must output PASS

    If any check fails: fix the issue and re-run before proceeding to the human checkpoint.
  </action>
  <verify>
    <automated>npm run lint && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>lint passes, build passes, .env absent from git history, all i18n keys verified</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Human verification — both pages + contact form end-to-end</name>
  <what-built>
    - Home-light page at / with 11 sections (hero, trust bar, what-is, how it works, for companies, anti-fraud, test library, testimonials, FAQ, contact form, footer)
    - Home-dark page at /dark — same 11 sections with dark/navy color scheme
    - POST /api/contact — rate-limited, validated, saves to Supabase, sends Resend email
    - npm run db:seed — populates 40 candidates, ~100 questions, 8 test templates
    - .env.schema with all 6 env var declarations
  </what-built>
  <how-to-verify>
    Start dev server: `npm run dev`

    **Page checks:**

    1. Visit http://localhost:3000 — confirm all 11 sections render, no blank sections, no layout breaks
    2. Click EN|FR language toggle — confirm ALL text on the page changes (no fallback key strings visible like "home_hero_headline")
    3. DevTools → responsive → 320px width — confirm no horizontal scrollbar appears on any section
    4. Scroll through all sections — confirm hero has floating dashboard card mockup, testimonials show 3-column grid on desktop, FAQ accordion opens/closes

    5. Visit http://localhost:3000/dark — confirm sections render with dark/navy color scheme, white text
    6. Click EN|FR toggle on /dark — confirm all text changes
    7. DevTools → 320px on /dark — no horizontal overflow

    **Contact form check:**

    8. Scroll to contact section on / — fill in all fields (name, company, email, team size, message)
    9. Tick the GDPR consent checkbox
    10. Click "Send message" — confirm success toast appears: "Message sent! We'll be in touch within 24 hours." and form resets to empty
    11. Check Supabase dashboard → contact_submissions table — new row appears
    12. Check ADMIN_EMAIL inbox — Resend notification email received with correct content

    **Rate limit check:**

    13. Submit the contact form 3 more times (total 4 submissions from same IP)
    14. On the 4th submission: confirm error toast appears: "Too many requests — please try again later."
    15. Confirm the 4th response body (check browser DevTools Network tab) contains only `{"error":"Too many requests"}` — no stack trace, no SQL details

    **Seed script check:**

    16. In a terminal: `npm run db:seed`
    17. Confirm exits 0 with log output showing row counts: ~40 candidates, ~100 questions, 8 templates
    18. Check Supabase dashboard → mock_candidates, questions, test_templates tables show expected row counts

    **SEC-01 check:**

    19. In a terminal: `git log --all --full-history -- .env`
    20. Confirm output is empty (no commits containing .env)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass.
    Or describe any issues found (e.g., "Hero card not visible on mobile", "FR translations missing in FAQ section") — Claude will create gap closure plans.
  </resume-signal>
</task>

</tasks>

<verification>
All automated checks pass (lint + build + i18n check + git history check). Human has visually confirmed both pages, the contact form end-to-end flow, rate limiting, seed script, and SEC-01 compliance.
</verification>

<success_criteria>
Phase 3 is complete when:
1. / and /dark render correctly on desktop and 320px mobile in both EN and FR
2. Contact form saves to Supabase and sends Resend email
3. Rate limiting triggers on the 4th submission with a generic error only
4. npm run db:seed exits 0 with 40 candidates, ~100 questions, 8 templates
5. .env has never been committed to git
6. .env.schema declares all 6 vars
</success_criteria>

<output>
After completion, create `.planning/phases/03-landing-pages-data-foundation/03-05-SUMMARY.md`
</output>

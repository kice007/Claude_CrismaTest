---
phase: 05-company-dashboard-api
plan: 09
type: execute
wave: 3
depends_on: [05-05, 05-06, 05-07, 05-08]
files_modified:
  - src/app/layout.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - locales/en.json
  - locales/fr.json
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06, DASH-07, DASH-08, DATA-04, DATA-05, DATA-05b, DATA-05c, DATA-06, DATA-07, DATA-08, DATA-09]

must_haves:
  truths:
    - "All 8 dashboard views render correctly in the browser without the global NavShell visible"
    - "Mobile layout at 375px shows bottom tab bar instead of sidebar"
    - "Candidate list email privacy confirmed via network inspection"
    - "All dashboard strings display in French when language is switched to FR"
    - "Auth redirect works — /dashboard while logged out goes to /login"
    - "All DASH-01 through DASH-08 requirements are verified by human"
    - "POST /api/contact satisfies DATA-09: zod validation, Resend trigger, rate-limiter, minimal error responses"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Final wired /dashboard page (after any gap fixes)"
  key_links:
    - from: "src/components/nav/NavShell.tsx"
      to: "dashboard pages"
      via: "route-based suppression check (implemented in Plan 04)"
      pattern: "usePathname.*dashboard"
---

<objective>
Integration pass and human verification checkpoint for all 8 dashboard views. Fix any i18n gaps or visual discrepancies found during final wire-up. Then pause for human visual QA.

Purpose: All Wave 2 plans run in parallel and may produce integration issues (missing i18n keys, layout gaps). This plan gathers those fixes and gates on human approval.
Output: All dashboard views confirmed working by human, any cross-cutting fixes applied.

Note: NavShell suppression on /dashboard/* routes is handled in Plan 04 (both fixed z-50 overlay and pathname early-return guard). This plan verifies it works but does NOT modify NavShell.tsx.
</objective>

<execution_context>
@C:/Users/chris/Downloads/Projects/CrismaTech/CrismTest/crismatest/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/Downloads/Projects/CrismaTech/CrismTest/crismatest/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-company-dashboard-api/05-CONTEXT.md
@.planning/phases/05-company-dashboard-api/05-04-SUMMARY.md
@.planning/phases/05-company-dashboard-api/05-05-SUMMARY.md
@.planning/phases/05-company-dashboard-api/05-06-SUMMARY.md
@.planning/phases/05-company-dashboard-api/05-07-SUMMARY.md
@.planning/phases/05-company-dashboard-api/05-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration fixes — i18n audit + build verification</name>
  <files>src/app/layout.tsx, locales/en.json, locales/fr.json</files>
  <action>
    Run the full project build and lint pass first:
    ```bash
    npm run lint && npx tsc --noEmit && npm run build
    ```

    Fix any errors found. Common integration issues to check:

    **1. NavShell suppression verification:**
    Plan 04 implements NavShell suppression via two mechanisms:
    - DashboardShell: `fixed inset-0 z-50` overlay
    - NavShell.tsx: `if (pathname.startsWith('/dashboard')) return null` early-return guard

    Verify both are in place by checking the files. If for any reason the NavShell is still visible on
    /dashboard/* routes in the browser, diagnose and fix. Do NOT re-add the pathname check if Plan 04
    already added it — check NavShell.tsx first.

    **2. i18n audit:**
    Scan all dashboard component files for any useTranslation() key calls that don't have matching entries in locales/en.json or locales/fr.json. Run:
    ```bash
    grep -r "t('" src/components/dashboard/ src/app/\(dashboard\)/ | grep -v ".test." | head -50
    ```
    Add any missing keys to both locale files. Keep French translations in formal vous register.

    **3. Mobile bottom padding:**
    Dashboard pages need padding-bottom on mobile to avoid content being hidden behind the BottomTabBar (56px / pb-14). Check if the DashboardShell main content area already applies this (Plan 04 adds `pb-14 md:pb-0`). If not present, add it to DashboardShell.tsx main content area.

    **4. Verify /dashboard route:**
    Check that `src/app/(dashboard)/dashboard/page.tsx` exists and is the final version from Plan 05 (not the stub from Plan 04). If the stub was left in place, confirm Plan 05 overwrote it.

    **5. DATA-09 audit — POST /api/contact:**
    Open `src/app/api/contact/route.ts` and verify it satisfies the DATA-09 spec in full:
    - Zod schema validates all required fields (name, email, message at minimum; any additional fields per Phase 3 design)
    - On validation failure: returns 400 with a minimal error message — NO stack traces, NO internal details
    - Calls Resend to send the contact email on success
    - Applies rate limiting: max 3 requests per IP per hour (check the rate-limiter middleware/utility from Phase 3)
    - Returns a minimal success response (200/201 with `{ success: true }` or similar — no verbose payload)

    If any of these are missing or incomplete, apply the fix directly to `src/app/api/contact/route.ts`. If the route fully satisfies the spec already, add a comment at the top of the route file:
    ```typescript
    // DATA-09: verified — zod validation, Resend trigger, rate-limiter, minimal error responses
    ```

    After all fixes, run the full build again:
    ```bash
    npm run lint && npx tsc --noEmit && npm run build
    ```
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Build, lint, and type-check all pass. No TypeScript errors. No missing i18n keys in dashboard components. POST /api/contact confirmed to satisfy DATA-09 (zod, Resend, rate-limiter, minimal errors) — either already correct or fixed and documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 5 dashboard — 8 views across 6 routes, 7 API endpoints, DashboardShell with sidebar + mobile tab bar, all wired to live Supabase data.

    Built in this phase:
    - /dashboard — candidate list table with search, filters, row selection, compare CTA
    - /dashboard/candidates/[id] — detail with animated score gauge, sub-scores, fraud flags, AI insight, mailto
    - /dashboard/compare?ids=... — 3-column comparison with "Recommended for Interview" badge
    - /dashboard/talent-pool — filterable card grid with contact modal
    - /dashboard/tests — test list with build CTA, delete confirm, search
    - /dashboard/tests/[id] — test detail, edit mode, delete, share modal
    - /dashboard/build-test — 4-step wizard, custom question modal, success modal with copy link
    - Mobile: sidebar → bottom tab bar (56px, brand-navy) at 375px
  </what-built>
  <how-to-verify>
    Start the dev server: `npm run dev`

    **Core flows to verify:**

    1. Auth gate: visit http://localhost:3000/dashboard while logged out → should redirect to /login
    2. Log in via /login → redirected to /dashboard

    3. Candidate list (/dashboard):
       - Table loads with candidate data
       - Search by name filters results
       - Global NavShell is NOT visible (covered by dashboard chrome)
       - Select 2+ rows → "Compare Selected" button appears
       - Click row → navigates to /dashboard/candidates/{id}

    4. Candidate detail (/dashboard/candidates/{id}):
       - Score gauge animates from 0 to score value
       - Sub-score bars display correctly
       - Email field is a mailto: link (click opens email client)
       - "Invite to Interview" opens calendar modal

    5. Compare (/dashboard → select 2-3 → Compare Selected):
       - 2-3 columns of candidate data
       - Highest scorer has "Recommended for Interview" badge
       - Export Report downloads a PDF file (check Downloads folder)

    6. Talent pool (/dashboard/talent-pool):
       - Card grid loads (4 cols desktop, 2 tablet, 1 mobile at 375px)
       - "Contact Candidate" opens contact modal

    7. **DASH-07 — Tests list (/dashboard/tests):**
       - Test list loads from /api/tests (check Network tab to confirm the API call)
       - Each test row shows name, role, date, status, and action buttons
       - "Build New Test" button goes to /dashboard/build-test
       - Delete icon → confirm modal → deletes test and removes from list
       - If no tests exist: empty state shows a "Build New Test" CTA (not a blank page)

    8. Build test (/dashboard/build-test):
       - Step 1: role selection
       - Step 2: module selection (check back button preserves role)
       - Step 3: add a custom question via the modal (desktop Dialog)
       - Step 4: generate → success modal
       - Copy Test Link → toast appears
       - View Test → navigates to /dashboard/tests/{newId}

    9. **DASH-08 — Test detail (/dashboard/tests/{id}):**
       - Full detail view loads: questions preview, shareable link, candidate responses count
       - Edit mode: click Edit → form becomes editable → change test name → save → success modal appears
       - Delete: confirm modal → test deleted → redirected away
       - Share modal: opens with copy link button → click copies link to clipboard

    10. Mobile at 375px (DevTools responsive mode):
        - Sidebar NOT visible
        - Bottom tab bar visible at bottom (5 icons, 56px height, brand-navy)
        - Candidate table becomes card stack
        - Tab bar navigation works between views

    11. i18n: switch to FR via language toggle → all dashboard text changes to French

    12. API privacy check (optional): open DevTools → Network tab → filter for /api/candidates → verify response JSON has no "email" field in list items; /api/candidates/{id} DOES have email

    13. **DATA-09 — POST /api/contact:**
        Run in terminal: `curl -X POST http://localhost:3000/api/contact -H "Content-Type: application/json" -d '{"name":"Test","email":"test@example.com","message":"Hello"}'`
        - Should return 200/201 with a minimal success response (no verbose payload)
        Run with missing fields: `curl -X POST http://localhost:3000/api/contact -H "Content-Type: application/json" -d '{}'`
        - Should return 400 with a short error message — NO stack trace in the response body
  </how-to-verify>
  <resume-signal>Type "approved" if all verified, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human verification is the gate for this plan. All 8 dashboard views must be confirmed working visually. Automated: full build must pass before presenting the checkpoint.
</verification>

<success_criteria>
Phase 5 complete when human approves all 8 dashboard views, mobile layout, i18n switching, and auth redirect. All requirement IDs DASH-01 through DASH-08, DATA-04 through DATA-09 (including DATA-05b, DATA-05c) verified. DASH-07 (tests list from /api/tests, empty state CTA) and DASH-08 (test detail, edit mode, delete, share modal) explicitly confirmed. DATA-09 (POST /api/contact: zod, Resend, rate-limiter, minimal errors) explicitly confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-company-dashboard-api/05-09-SUMMARY.md`
</output>

---
phase: 05-company-dashboard-api
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - src/app/api/candidates/route.ts
  - src/app/api/candidates/[id]/route.ts
  - src/app/api/talent-pool/route.ts
autonomous: true
requirements: [DATA-06, DATA-07, DATA-08]

must_haves:
  truths:
    - "GET /api/candidates returns candidate list with no email field"
    - "GET /api/candidates/[id] returns full candidate detail including email"
    - "GET /api/talent-pool returns talent pool entries"
    - "All error responses are { error: string } with no stack traces"
  artifacts:
    - path: "src/app/api/candidates/route.ts"
      provides: "GET /api/candidates — candidate list without email"
      exports: ["GET"]
    - path: "src/app/api/candidates/[id]/route.ts"
      provides: "GET /api/candidates/[id] — full candidate including email"
      exports: ["GET"]
    - path: "src/app/api/talent-pool/route.ts"
      provides: "GET /api/talent-pool — filterable pool"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/candidates/route.ts"
      to: "supabase mock_candidates table"
      via: "createServerSupabaseClient()"
      pattern: "mock_candidates.*select"
    - from: "src/app/api/candidates/[id]/route.ts"
      to: "supabase mock_candidates table"
      via: "createServerSupabaseClient()"
      pattern: "mock_candidates.*eq.*id"
---

<objective>
Create three Supabase-backed API routes: GET /api/candidates (list, no email), GET /api/candidates/[id] (full detail with email), and GET /api/talent-pool (filterable candidates).

Purpose: These routes are consumed by the dashboard candidate views built in Wave 2. They can be built and verified independently before any UI exists.
Output: 3 route handler files following the established /api/contact pattern.
</objective>

<execution_context>
@C:/Users/chris/Downloads/Projects/CrismaTech/CrismTest/crismatest/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/Downloads/Projects/CrismaTech/CrismTest/crismatest/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-company-dashboard-api/05-CONTEXT.md
@.planning/phases/05-company-dashboard-api/05-RESEARCH.md
@src/app/api/contact/route.ts
@src/lib/supabase.ts

<interfaces>
<!-- Established API route pattern (from src/app/api/contact/route.ts) -->
```typescript
import { NextRequest, NextResponse } from 'next/server'
import { createServerSupabaseClient } from '@/lib/supabase'

export async function GET(req: NextRequest) {
  try {
    const supabase = await createServerSupabaseClient()
    const { data, error } = await supabase.from('table').select('col1, col2')
    if (error) {
      console.error('[route] supabase error:', error.message)
      return NextResponse.json({ error: 'Internal server error' }, { status: 500 })
    }
    return NextResponse.json(data)
  } catch {
    return NextResponse.json({ error: 'Internal server error' }, { status: 500 })
  }
}
```

<!-- mock_candidates table fields (from RESEARCH.md + Phase 3 seed) -->
Available columns: id, full_name, role, avatar_initials, avatar_color, crima_score, trust_score,
logic_score, comms_score, job_skill_score, fraud_flag_severity, fraud_flag_count, status,
test_date, email, phone, company, job_title
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET /api/candidates + GET /api/candidates/[id]</name>
  <files>src/app/api/candidates/route.ts, src/app/api/candidates/[id]/route.ts</files>
  <action>
    Create src/app/api/candidates/route.ts:
    - Method: GET
    - Supabase query: `supabase.from('mock_candidates').select('id, full_name, role, avatar_initials, avatar_color, crima_score, trust_score, fraud_flag_severity, fraud_flag_count, status, test_date').order('test_date', { ascending: false })`
    - CRITICAL: email column is intentionally OMITTED from SELECT — this is the DATA-06 privacy requirement enforced at query level, not stripped in-handler
    - Support optional query params for filtering: `?role=`, `?status=`, `?search=` (apply .ilike() on full_name if search provided), `?score_min=`, `?score_max=`
    - Error response: `{ error: 'Internal server error' }` — no stack traces, no field details
    - Success response: array of candidate objects

    Create src/app/api/candidates/[id]/route.ts:
    - Method: GET
    - Route segment: the `id` param comes from `params.id` via the Next.js route params pattern
    - Supabase query: `supabase.from('mock_candidates').select('*').eq('id', id).single()`
    - Returns full candidate including email field (DATA-07 — detail view needs mailto link)
    - If no row found: `{ error: 'Not found' }` with status 404
    - Error response pattern: same `{ error: string }` minimal format

    Both files must follow the exact pattern from src/app/api/contact/route.ts — try/catch wrapping the entire handler, console.error for Supabase errors (includes route prefix like '[candidates]'), no internal info in client responses.

    Use `createServerSupabaseClient` from `@/lib/supabase` (server variant — do NOT use createBrowserClient in route handlers).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    GET /api/candidates responds 200 with array (no email field in objects).
    GET /api/candidates/[id] responds 200 with full object including email.
    Both return { error: string } on failure.
    Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: GET /api/talent-pool</name>
  <files>src/app/api/talent-pool/route.ts</files>
  <action>
    Create src/app/api/talent-pool/route.ts:
    - Method: GET
    - The talent pool is drawn from the same mock_candidates table — no separate table exists (confirmed in RESEARCH.md)
    - Filter: query candidates where status = 'passed' OR crima_score >= 60 (reasonable pool threshold) to differentiate from the main candidate table
    - Supabase query: `supabase.from('mock_candidates').select('id, full_name, role, avatar_initials, avatar_color, crima_score, trust_score, status, test_date').gte('crima_score', 60).order('crima_score', { ascending: false })`
    - Support optional query params: `?role=`, `?score_min=`, `?score_max=`, `?search=` (ilike on full_name)
    - Email intentionally excluded from SELECT (pool list view — privacy consistent with candidate list)
    - Error response: `{ error: 'Internal server error' }`, no stack traces
    - Success response: array of talent pool entries

    Follow the same established pattern as Task 1.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>GET /api/talent-pool responds 200 with array of candidates with crima_score >= 60, no email field, build passes</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run build` passes
2. `curl http://localhost:3000/api/candidates` returns array without any email field
3. `curl http://localhost:3000/api/candidates/{id}` returns object with email field
4. `curl http://localhost:3000/api/talent-pool` returns array of higher-scoring candidates
5. `curl http://localhost:3000/api/candidates/nonexistent-id` returns `{"error":"Not found"}` with 404
</verification>

<success_criteria>
3 API routes operational and returning Supabase data. Email privacy enforced at query level (not stripped in JS). All error responses are minimal `{ error: string }`. Build green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-company-dashboard-api/05-02-SUMMARY.md`
</output>

---
phase: 05-company-dashboard-api
plan: 06
type: execute
wave: 2
depends_on: [05-02, 05-04]
files_modified:
  - src/app/(dashboard)/dashboard/compare/page.tsx
  - src/components/dashboard/CompareColumn.tsx
  - src/app/(dashboard)/dashboard/talent-pool/page.tsx
  - src/components/dashboard/TalentPoolCard.tsx
  - src/components/dashboard/FilterSheet.tsx
autonomous: true
requirements: [DASH-04, DASH-05]

must_haves:
  truths:
    - "Compare view reads ?ids= query param, fetches each candidate, renders 3-column layout"
    - "Top scorer receives Recommended for Interview badge; ties mean both get the badge"
    - "Export Report button downloads /public/crima-compare-report.pdf"
    - "Talent pool card grid shows 4 cols desktop / 2 tablet / 1 mobile"
    - "Card click opens ContactModal with candidate name; email prefills once secondary fetch resolves"
    - "Talent pool filters work for role, score range, search"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/compare/page.tsx"
      provides: "DASH-04 compare view (3-column candidate comparison)"
    - path: "src/components/dashboard/CompareColumn.tsx"
      provides: "Single candidate column: gauge + sub-scores + fraud flags"
    - path: "src/app/(dashboard)/dashboard/talent-pool/page.tsx"
      provides: "DASH-05 talent pool card grid"
    - path: "src/components/dashboard/TalentPoolCard.tsx"
      provides: "Individual talent pool candidate card"
    - path: "src/components/dashboard/FilterSheet.tsx"
      provides: "Mobile bottom sheet for filters"
  key_links:
    - from: "src/app/(dashboard)/dashboard/compare/page.tsx"
      to: "/api/candidates/[id]"
      via: "Promise.all fetch"
      pattern: "Promise.all.*fetch.*candidates"
    - from: "src/app/(dashboard)/dashboard/talent-pool/page.tsx"
      to: "/api/talent-pool"
      via: "fetch in useEffect"
      pattern: "fetch.*api/talent-pool"
    - from: "src/app/(dashboard)/dashboard/talent-pool/page.tsx"
      to: "/api/candidates/[id]"
      via: "secondary fetch on card click to retrieve email"
      pattern: "fetch.*api/candidates.*id"
---

<objective>
Build the compare view (DASH-04) and the talent pool view (DASH-05) — two independent views that can be implemented in parallel within this plan since they share no files.

Purpose: Compare completes the candidate evaluation flow. Talent pool is a separate browse experience. Both depend on Wave 1 API routes and the DashboardShell from Plan 04.
Output: 2 page files + 3 component files.

**DASH-05 contact modal resolution:** CONTEXT.md locks the ContactModal pattern — card click opens the existing `ContactModal.tsx` (Phase 2) with candidate name + prefilled email. REQUIREMENTS.md mentions "inline detail modal" which is satisfied by the ContactModal being triggered on card click. The ContactModal IS the inline detail modal for v1. Sub-scores and fraud flags are visible on the /dashboard/candidates/[id] detail page (DASH-03, Plan 05) — the talent pool card click intentionally goes to ContactModal only, not a full detail panel. This is the locked decision per CONTEXT.md.
</objective>

<execution_context>
@C:/Users/chris/Downloads/Projects/CrismaTech/CrismTest/crismatest/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/Downloads/Projects/CrismaTech/CrismTest/crismatest/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-company-dashboard-api/05-CONTEXT.md
@.planning/phases/05-company-dashboard-api/05-RESEARCH.md
@.planning/phases/05-company-dashboard-api/05-02-SUMMARY.md
@.planning/phases/05-company-dashboard-api/05-04-SUMMARY.md
@src/components/modals/ContactModal.tsx

<interfaces>
<!-- Compare view locked decisions from CONTEXT.md -->
// State: query params /dashboard/compare?ids=a,b,c
// Badge logic: top scorer = highest crima_score; ties = both get badge
// Export: downloads /public/crima-compare-report.pdf (static file, no dynamic generation)
// Back: router.push('/dashboard') — preserves nothing (selection state was in /dashboard local state)
// Columns stack vertically on mobile

<!-- ContactModal interface (from Phase 2) -->
```tsx
// src/components/modals/ContactModal.tsx
interface ContactModalProps {
  open: boolean
  onOpenChange: (open: boolean) => void
  candidateName?: string
  candidateEmail?: string
}
```

<!-- TalentPoolCard candidate shape (from GET /api/talent-pool — same as candidates list minus email) -->
```typescript
interface TalentPoolEntry {
  id: string
  full_name: string
  role: string
  avatar_initials: string
  avatar_color: string
  crima_score: number
  trust_score: number
  status: string
  test_date: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compare view (/dashboard/compare)</name>
  <files>src/app/(dashboard)/dashboard/compare/page.tsx, src/components/dashboard/CompareColumn.tsx</files>
  <action>
    Create src/components/dashboard/CompareColumn.tsx:
    - 'use client' directive
    - Props: `{ candidate: CandidateDetail, isRecommended: boolean }`
    - Renders single candidate column for the compare layout
    - Top: avatar + name + role
    - If isRecommended: "Recommended for Interview" badge (brand-primary, prominent) — dashboard.compare.recommended i18n key
    - ScoreGauge (import from Plan 05 — src/components/dashboard/ScoreGauge.tsx)
    - SubScoreBars (import from Plan 05 — src/components/dashboard/SubScoreBars.tsx)
    - FraudFlagsPanel (import from Plan 05 — src/components/dashboard/FraudFlagsPanel.tsx)
    - Small video thumbnail placeholder: gray rectangle 16:9 with play button overlay (mocked — no real video)
    - On mobile: component renders full-width (stacking is handled at page level)

    Create src/app/(dashboard)/dashboard/compare/page.tsx:
    - 'use client' directive
    - Read query params: `useSearchParams().get('ids')?.split(',') ?? []`
    - If no ids: show "No candidates selected" message with "Back to Candidates" link
    - If ids present: `Promise.all(ids.map(id => fetch('/api/candidates/' + id).then(r => r.json())))`
    - State: candidates (CandidateDetail[]), loading (boolean), error (string | null)
    - Badge logic: find max crima_score among candidates; mark ALL candidates with that score as recommended (handles ties)
    - Layout:
      - Page header: "Compare Candidates" title + "Export Report" button + "Back" link
      - "Export Report": `<a href="/crima-compare-report.pdf" download>` — downloads the static PDF from Plan 01
      - Grid: 3-column on desktop (grid-cols-3), 1-column on mobile (grid-cols-1)
      - Map candidates → CompareColumn (pass isRecommended flag)
      - If < 3 candidates: remaining columns show empty "Add candidate" placeholder (gray dashed border)
    - Skeleton loading: 3 CompareColumn skeletons while loading
    - All strings via useTranslation() using dashboard.compare i18n keys
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    /dashboard/compare?ids=a,b renders 2-column layout fetching both candidates. Top scorer has badge. Export Report link downloads PDF. No ids shows empty state message. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Talent pool view (/dashboard/talent-pool)</name>
  <files>src/app/(dashboard)/dashboard/talent-pool/page.tsx, src/components/dashboard/TalentPoolCard.tsx, src/components/dashboard/FilterSheet.tsx</files>
  <action>
    Create src/components/dashboard/TalentPoolCard.tsx:
    - 'use client' directive
    - Props: `{ candidate: TalentPoolEntry, onClick: () => void }`
    - Card layout:
      - Avatar (colored circle with initials)
      - Full name (bold)
      - Role (slate-600)
      - CrismaScore badge (color-coded as in candidate table)
      - TrustScore (smaller text)
      - Last test date
      - "Contact Candidate" button (brand-primary outlined button, 48px min height per UI-07)
    - onClick: triggers the contact modal from the parent page
    - Hover state: subtle shadow lift (shadow-md transition)

    Create src/components/dashboard/FilterSheet.tsx:
    - 'use client' directive — mobile bottom sheet for filters
    - Uses vaul Drawer (import from 'vaul') — consistent with Phase 2 modal pattern
    - Props: `{ open: boolean, onOpenChange: (open: boolean) => void, filters: FilterState, onChange: (filters: FilterState) => void }`
    - FilterState: `{ role: string, scoreMin: string, scoreMax: string, search: string }`
    - Contains: role Select, score range inputs (min/max), apply button
    - Only shown on mobile — desktop shows inline filter bar

    Create src/app/(dashboard)/dashboard/talent-pool/page.tsx:
    - 'use client' directive
    - State: candidates (TalentPoolEntry[]), loading (boolean), filters (FilterState), contactTarget (TalentPoolEntry | null), contactEmail (string | null), contactEmailLoading (boolean), filterSheetOpen (boolean)
    - On mount + filter change: fetch('/api/talent-pool' + buildQueryString(filters))
    - Layout:
      - Page header: "Talent Pool" title
      - Desktop filter bar (hidden on mobile): search Input + role Select + score range inputs
      - Mobile filter button (visible on mobile only): "Filters" button with Filter icon → opens FilterSheet
      - Card grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4` — matches DASH-05 spec (4 cols desktop / 2 tablet / 1 mobile)
      - TalentPoolCard onClick handler: open the ContactModal immediately, then fetch email in the background:
        ```
        setContactTarget(candidate)
        setContactEmailLoading(true)
        setContactEmail(null)
        fetch('/api/candidates/' + candidate.id)
          .then(r => r.ok ? r.json() : null)
          .then(full => setContactEmail(full?.email ?? null))
          .finally(() => setContactEmailLoading(false))
        ```
      - ContactModal (reuse from Phase 2 — src/components/modals/ContactModal.tsx): open when contactTarget != null;
        pass candidateName={contactTarget.full_name} and candidateEmail={contactEmail ?? undefined}
      - While contactEmailLoading is true, the mailto button inside ContactModal is disabled (the modal
        is visible immediately — the email field populates once the secondary fetch resolves).
        If the secondary fetch fails or returns no email, the modal remains open without email pre-fill
        (graceful degradation).
      - DASH-05 spec: GET /api/talent-pool excludes email per DATA-08 privacy; the secondary
        GET /api/candidates/[id] retrieves the full candidate including email for the mailto prefill.
      - Note: The ContactModal IS the inline detail modal for DASH-05 (v1 scope per CONTEXT.md locked decision).
        Full sub-score detail for talent pool candidates is accessible via /dashboard/candidates/[id] (DASH-03).
    - Skeleton: 8 card skeletons while loading
    - Empty state: EmptyState component with dashboard.talentPool.empty text
    - All strings via useTranslation() using dashboard.talentPool keys
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    /dashboard/talent-pool loads card grid from API. 4-col desktop / 2-col tablet / 1-col mobile layout correct. Card click opens ContactModal immediately; mailto is prefilled with email once secondary fetch resolves. Mobile filter sheet works. Build passes.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run build` passes
2. /dashboard/compare?ids=a,b shows 2-column layout with badge on top scorer
3. If both candidates have the same score, both get the badge
4. Export Report downloads the PDF
5. /dashboard/talent-pool shows card grid with correct responsive layout
6. Card click opens ContactModal immediately; email prefills once secondary fetch resolves
7. Talent pool filters update the card grid
</verification>

<success_criteria>
Compare (DASH-04) and talent pool (DASH-05) views fully functional. Badge logic handles ties correctly. Static PDF download works. Card grid is responsive. ContactModal mailto is prefilled via secondary GET /api/candidates/[id]. Build green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-company-dashboard-api/05-06-SUMMARY.md`
</output>

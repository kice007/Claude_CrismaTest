---
phase: 05-company-dashboard-api
plan: 05
type: execute
wave: 2
depends_on: [05-02, 05-04]
files_modified:
  - src/app/(dashboard)/dashboard/page.tsx
  - src/components/dashboard/CandidateTable.tsx
  - src/components/dashboard/CandidateCard.tsx
  - src/components/dashboard/ScoreGauge.tsx
  - src/app/(dashboard)/dashboard/candidates/[id]/page.tsx
  - src/components/dashboard/SubScoreBars.tsx
  - src/components/dashboard/FraudFlagsPanel.tsx
  - src/components/dashboard/AIInsightCard.tsx
autonomous: true
requirements: [DASH-01, DASH-03]

must_haves:
  truths:
    - "Candidate list table loads from GET /api/candidates with skeleton loading state"
    - "Search by name/email and filters for role, score, status, date work in the table"
    - "Row click navigates to /dashboard/candidates/[id]"
    - "Candidate detail shows score gauge, sub-scores, fraud flags, AI insight card"
    - "Email on detail page is a clickable mailto: link"
    - "Checkboxes allow selecting 2-3 candidates; Compare Selected button appears when ≥2 selected"
    - "Skeleton loading shows while data fetches; EmptyState shows when list is empty"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "DASH-01 candidates list view"
      min_lines: 80
    - path: "src/components/dashboard/CandidateTable.tsx"
      provides: "Data table with search, filters, row selection"
    - path: "src/components/dashboard/ScoreGauge.tsx"
      provides: "Animated circular CrismaScore gauge (SVG strokeDasharray)"
    - path: "src/app/(dashboard)/dashboard/candidates/[id]/page.tsx"
      provides: "DASH-03 candidate detail view"
    - path: "src/components/dashboard/SubScoreBars.tsx"
      provides: "5 horizontal sub-score progress bars"
    - path: "src/components/dashboard/FraudFlagsPanel.tsx"
      provides: "Anti-fraud severity badges + flag list"
    - path: "src/components/dashboard/AIInsightCard.tsx"
      provides: "3-bullet AI interview recommendation card"
  key_links:
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "/api/candidates"
      via: "fetch in useEffect"
      pattern: "fetch.*api/candidates"
    - from: "src/app/(dashboard)/dashboard/candidates/[id]/page.tsx"
      to: "/api/candidates/[id]"
      via: "fetch in useEffect"
      pattern: "fetch.*api/candidates.*id"
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "/dashboard/compare"
      via: "router.push with query params"
      pattern: "router.push.*compare.*ids"
---

<objective>
Build the two candidate-focused dashboard views: the candidate list table (DASH-01) and the candidate detail page (DASH-03), plus the shared components they require (ScoreGauge, SubScoreBars, FraudFlagsPanel, AIInsightCard).

Purpose: These are the primary dashboard views. The compare flow (Plan 07) depends on the checkbox selection state pattern established here.
Output: 2 page files + 6 component files, all wired to API routes from Plan 02.
</objective>

<execution_context>
@C:/Users/chris/Downloads/Projects/CrismaTech/CrismTest/crismatest/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/Downloads/Projects/CrismaTech/CrismTest/crismatest/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-company-dashboard-api/05-CONTEXT.md
@.planning/phases/05-company-dashboard-api/05-RESEARCH.md
@.planning/phases/05-company-dashboard-api/05-02-SUMMARY.md
@.planning/phases/05-company-dashboard-api/05-04-SUMMARY.md
@src/components/Skeleton.tsx
@src/components/EmptyState.tsx
@src/components/modals/InviteModal.tsx
@src/components/modals/CalendarModal.tsx

<interfaces>
<!-- API response shapes from Plan 02 -->
```typescript
// GET /api/candidates item:
interface Candidate {
  id: string
  full_name: string
  role: string
  avatar_initials: string
  avatar_color: string
  crima_score: number
  trust_score: number
  fraud_flag_severity: 'none' | 'low' | 'medium' | 'high'
  fraud_flag_count: number
  status: 'passed' | 'failed' | 'pending'
  test_date: string
}

// GET /api/candidates/[id] adds:
interface CandidateDetail extends Candidate {
  email: string
  phone: string
  company: string
  job_title: string
  logic_score: number
  comms_score: number
  job_skill_score: number
}
```

<!-- shadcn Table usage (installed in Plan 01) -->
```tsx
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table'
import { Badge } from '@/components/ui/badge'
import { Input } from '@/components/ui/input'
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
import { Checkbox } from '@/components/ui/checkbox'
```

<!-- Framer Motion variants (use animations.ts, NOT motion.ts) -->
import { fadeIn, slideUp, staggerChildren } from '@/lib/animations'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /dashboard — candidate list table</name>
  <files>src/app/(dashboard)/dashboard/page.tsx, src/components/dashboard/CandidateTable.tsx, src/components/dashboard/CandidateCard.tsx</files>
  <action>
    Create src/components/dashboard/CandidateTable.tsx:
    - 'use client' directive
    - Props: `{ candidates: Candidate[], onRowClick: (id: string) => void, onSelectionChange: (ids: string[]) => void }`
    - Renders shadcn Table with columns: checkbox, avatar (colored initials circle), name, role, CrismaScore chip (shadcn Badge with color based on score ≥70: green, 50-69: amber, <50: red), TrustScore (plain number + /100), status Badge (passed=green, failed=red, pending=gray), test date (formatted), actions (eye icon → row click)
    - Checkbox column: each row has a Checkbox; header has "select all" checkbox
    - Selection state: controlled via onSelectionChange prop
    - Row click on non-checkbox cells: calls onRowClick(id)
    - CrismaScore chip: use shadcn Badge with custom className for color coding
    - Shows Skeleton shimmer when no data passed (loading prop)
    - Shows EmptyState when data is empty array (import from @/components/EmptyState)

    Create src/components/dashboard/CandidateCard.tsx:
    - 'use client' directive — mobile card representation of a candidate row
    - Props: `{ candidate: Candidate, selected: boolean, onSelect: () => void, onClick: () => void }`
    - Card layout (used on mobile when table becomes card stack):
      - Top row: avatar circle + name + role
      - Score row: CrismaScore badge + TrustScore + status badge
      - Bottom row: test date + checkbox for selection
    - 48px minimum tap target on click area (UI-07)

    Create src/app/(dashboard)/dashboard/page.tsx:
    - 'use client' directive
    - State: candidates (Candidate[]), loading (boolean), selectedIds (string[]), search (string), filters (role, status, scoreMin, scoreMax)
    - On mount: fetch('/api/candidates') → populate candidates (with query params for active filters)
    - Re-fetch when filters change (debounce search by 300ms)
    - "Compare Selected" button: visible when selectedIds.length >= 2; onClick → router.push(`/dashboard/compare?ids=${selectedIds.join(',')}`)
    - "Invite Candidate" button: opens InviteModal (import from @/components/modals/InviteModal)
    - Layout:
      - Page header: "Candidates" title (dashboard.candidateTable.title) + Invite Candidate button
      - Filter bar: search Input + role Select + status Select + score range (two number inputs or a range select)
      - Compare Selected button (conditionally rendered above table)
      - CandidateTable (desktop) — hidden on mobile
      - CandidateCard stack (mobile) — hidden on desktop: map candidates to CandidateCard
    - Row click: router.push(`/dashboard/candidates/${id}`)
    - All strings via useTranslation('translation') using dashboard.candidateTable keys
    - Skeleton loading: show 5 CandidateTable skeleton rows (using Skeleton component) while loading=true
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    /dashboard loads candidate table from API. Search and filters apply as query params. Selecting 2+ rows shows "Compare Selected" button. Row click navigates to detail. Mobile shows card stack. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: /dashboard/candidates/[id] — candidate detail + shared gauge/sub-score components</name>
  <files>
    src/app/(dashboard)/dashboard/candidates/[id]/page.tsx,
    src/components/dashboard/ScoreGauge.tsx,
    src/components/dashboard/SubScoreBars.tsx,
    src/components/dashboard/FraudFlagsPanel.tsx,
    src/components/dashboard/AIInsightCard.tsx
  </files>
  <action>
    Create src/components/dashboard/ScoreGauge.tsx:
    - 'use client' directive
    - Props: `{ score: number, size?: number }` (default size: 160)
    - SVG circle gauge: cx=size/2, cy=size/2, r=(size/2 - 10)
    - strokeDasharray: circumference = 2 * π * r; strokeDashoffset = circumference * (1 - score/100)
    - Animate strokeDashoffset from circumference to final value using Framer Motion (1.5s ease-out)
    - Score number centered in circle (JetBrains Mono font, large)
    - Track stroke: slate-200, progress stroke: brand-primary (#1B4FD8)
    - Grade badge below score (A: ≥90, B: 70-89, C: 50-69, D: <50)

    Create src/components/dashboard/SubScoreBars.tsx:
    - Props: `{ logic: number, communication: number, jobSkill: number, trust: number, video: number }`
    - 5 horizontal progress bars, each with label and score/100
    - Bar fill: brand-primary, track: slate-100
    - Labels from dashboard.candidateDetail.scores i18n keys

    Create src/components/dashboard/FraudFlagsPanel.tsx:
    - Props: `{ severity: 'none' | 'low' | 'medium' | 'high', flagCount: number, flags?: string[] }`
    - Header badge: severity label with color (none=slate, low=green, medium=amber, high=red)
    - Flag list: bullet list of flag descriptions (use mock flag descriptions if flags array not provided)
    - Mock flag descriptions: ["Tab switch detected (2×)", "Eye movement anomaly", "Multiple faces detected"]

    Create src/components/dashboard/AIInsightCard.tsx:
    - Props: `{ candidateName: string, score: number }`
    - Card with brand-light background, "AI Interview Recommendation" header
    - 3 mocked bullet points based on score range:
      - score ≥ 70: positive bullets ("Strong analytical reasoning...", "Excellent communication...", "Recommended for senior role...")
      - score 50-69: mixed bullets ("Solid foundational skills...", "May benefit from...", "Consider for...")
      - score < 50: cautious bullets ("Further evaluation recommended...", "Shows potential in...", "Consider entry-level...")
    - No real AI calls — purely mocked based on score threshold

    Create src/app/(dashboard)/dashboard/candidates/[id]/page.tsx:
    - 'use client' directive
    - Reads `params.id` — in Next.js App Router client components, use `use(params)` pattern:
      ```tsx
      import { use } from 'react'
      export default function CandidateDetailPage({ params }: { params: Promise<{ id: string }> }) {
        const { id } = use(params)
      ```
    - Fetch: `fetch('/api/candidates/' + id)` on mount
    - Layout (two columns on desktop, single column on mobile):
      - Left column: ScoreGauge + SubScoreBars + FraudFlagsPanel
      - Right column: header card (avatar, name, role, email as `<a href="mailto:email">`, test date, status badge) + AIInsightCard + action buttons
    - Action buttons:
      - "Invite to Interview" → opens CalendarModal (import from @/components/modals/CalendarModal)
      - "Email Candidate" → `window.location.href = 'mailto:${email}?subject=Interview+Invitation'`
    - Back link: "← Back to Candidates" → router.push('/dashboard')
    - Skeleton loading while data fetches
    - Error state: show "Candidate not found" with back link if API returns 404
    - All strings via useTranslation() using dashboard.candidateDetail keys
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    /dashboard/candidates/[id] loads full candidate data. ScoreGauge animates 0→final. Sub-scores displayed. Email is a mailto: link. Invite to Interview opens CalendarModal. Build passes.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run build` passes
2. /dashboard shows candidate table with data from API (verified with dev server running)
3. Search input filters results by name
4. Selecting 2+ candidates shows "Compare Selected" button
5. Row click navigates to /dashboard/candidates/{id}
6. Detail page shows animated score gauge and all sub-scores
7. Email field is a clickable mailto: link
8. "Invite to Interview" opens calendar modal
</verification>

<success_criteria>
Candidates list (DASH-01) and detail (DASH-03) pages fully functional. ScoreGauge, SubScoreBars, FraudFlagsPanel, AIInsightCard components built and reusable. Build green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-company-dashboard-api/05-05-SUMMARY.md`
</output>

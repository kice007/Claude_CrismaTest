---
phase: 05-company-dashboard-api
plan: 04
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - src/components/dashboard/DashboardShell.tsx
  - src/components/dashboard/Sidebar.tsx
  - src/components/dashboard/BottomTabBar.tsx
  - src/app/(dashboard)/layout.tsx
autonomous: true
requirements: [DASH-01, DASH-02]

must_haves:
  truths:
    - "DashboardShell renders as fixed inset-0 z-50 overlay covering the global NavShell"
    - "Sidebar shows 4 nav items + user section; active item highlighted with brand-primary"
    - "BottomTabBar shows 5 icons at 56px height with brand-navy background on mobile"
    - "Auth guard in layout.tsx redirects unauthenticated users to /login"
    - "Dashboard layout wraps all /dashboard/* pages without duplicating the auth guard"
  artifacts:
    - path: "src/components/dashboard/DashboardShell.tsx"
      provides: "Full-page dashboard layout container"
    - path: "src/components/dashboard/Sidebar.tsx"
      provides: "Desktop sidebar with nav + user profile at bottom"
    - path: "src/components/dashboard/BottomTabBar.tsx"
      provides: "Mobile 5-icon tab bar (56px, brand-navy)"
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Route group layout with auth guard + DashboardShell"
  key_links:
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/lib/auth-context.tsx"
      via: "useAuth().isLoggedIn"
      pattern: "useAuth.*isLoggedIn"
    - from: "src/components/dashboard/DashboardShell.tsx"
      to: "global NavShell"
      via: "fixed inset-0 z-50 overlay"
      pattern: "fixed.*inset-0.*z-50"
---

<objective>
Build the DashboardShell layout foundation: the sidebar (desktop), bottom tab bar (mobile), and the (dashboard) route group layout with auth guard.

Purpose: Every dashboard view built in Wave 2 lives inside this shell. It must be complete before any page is wired. The shell overlays the global NavShell using fixed positioning.
Output: DashboardShell, Sidebar, BottomTabBar components + (dashboard)/layout.tsx.
</objective>

<execution_context>
@C:/Users/chris/Downloads/Projects/CrismaTech/CrismTest/crismatest/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/Downloads/Projects/CrismaTech/CrismTest/crismatest/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-company-dashboard-api/05-CONTEXT.md
@.planning/phases/05-company-dashboard-api/05-RESEARCH.md
@src/app/layout.tsx
@src/lib/auth-context.tsx

<interfaces>
<!-- AuthContext interface -->
```typescript
interface AuthContextValue {
  isLoggedIn: boolean
  login: () => void
  logout: () => void
}
// Usage:
const { isLoggedIn, logout } = useAuth()
```

<!-- useMediaQuery hook (from Phase 2) -->
```typescript
// src/lib/use-media-query.ts
export function useMediaQuery(query: string): boolean
// Usage: const isMobile = useMediaQuery('(max-width: 768px)')
```

<!-- Motion variants (from src/lib/animations.ts — use this, NOT motion.ts) -->
```typescript
export const fadeIn: Variants
export const slideUp: Variants
export const staggerChildren: Variants
```

<!-- Sidebar nav items (locked in CONTEXT.md) -->
// 4 items: Dashboard → /dashboard, Candidates → /dashboard (same route),
// Tests → /dashboard/tests, Talent Pool → /dashboard/talent-pool
// User profile at bottom (avatar + plan badge) — not a nav item
// Mobile tab bar: 5 icons — Dashboard, Candidates, Tests, Talent Pool, Profile
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar + BottomTabBar components</name>
  <files>src/components/dashboard/Sidebar.tsx, src/components/dashboard/BottomTabBar.tsx</files>
  <action>
    Create src/components/dashboard/Sidebar.tsx:
    - 'use client' directive (uses usePathname, useAuth, useTranslation)
    - Brand-navy background (#0F2A6B / bg-[#0F2A6B] or Tailwind brand-navy if available)
    - Width: 240px (w-60) on desktop, hidden on mobile (hidden md:flex)
    - Full height: h-full, flex flex-col
    - Top: Logo area (CrismaTest wordmark, white text) with a subtle bottom border
    - Nav items (middle section, flex-1): use dashboard.nav i18n keys
      - Dashboard → /dashboard (LayoutDashboard icon from lucide-react)
      - Candidates → /dashboard (Users icon)
      - Tests → /dashboard/tests (FileText icon)
      - Talent Pool → /dashboard/talent-pool (Star icon)
      - Active state: brand-primary left border + brand-primary/10 background + white text
      - Inactive state: slate-300 text, hover: white/10 background
      - Use usePathname() to determine active item (exact match for /dashboard, startsWith for others)
    - Bottom section: user avatar + name "John Doe" + plan badge ("Pro Plan" from dashboard.sidebar.plan key) + logout button (LogOut icon, triggers useAuth().logout())
    - Note: "John Doe" is the static placeholder per Phase 4 decision (real user data deferred to Phase 5 but context says static is fine for v1)

    Create src/components/dashboard/BottomTabBar.tsx:
    - 'use client' directive
    - Renders on mobile only (flex md:hidden)
    - Fixed bottom-0 left-0 right-0 (position: fixed)
    - Height: 56px (h-14)
    - Brand-navy background
    - 5 equal-width icon buttons: Dashboard, Candidates, Tests, Talent Pool, Profile
    - Icon + label below (12px text)
    - Active: brand-primary color, inactive: slate-300
    - Profile tab: opens nothing yet (no profile page in v1) — noop or show toast "Profile coming soon"
    - All labels use dashboard.nav i18n keys
    - Use lucide-react icons: LayoutDashboard, Users, FileText, Star, User

    Both components: all strings via useTranslation(), referencing the "dashboard" namespace keys pre-loaded in Plan 01.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Sidebar and BottomTabBar components compile without TypeScript errors. All strings use useTranslation() — no hardcoded English text in JSX.</done>
</task>

<task type="auto">
  <name>Task 2: DashboardShell + (dashboard)/layout.tsx</name>
  <files>src/components/dashboard/DashboardShell.tsx, src/app/(dashboard)/layout.tsx</files>
  <action>
    Create src/components/dashboard/DashboardShell.tsx:
    - 'use client' directive
    - Renders as `fixed inset-0 z-50 bg-white flex` — this overlays the global NavShell (confirmed approach from RESEARCH.md Pitfall 1)
    - Layout: Sidebar on left (240px, shrink-0), main content area fills remaining width
    - Main content: flex-1 overflow-y-auto (scrollable), padding: p-6 (inner content padding)
    - BottomTabBar rendered at bottom of the fixed container (it positions itself as fixed bottom-0)
    - Mobile: on screens < md, the sidebar is hidden (handled in Sidebar.tsx itself) and BottomTabBar appears
    - Props: `{ children: React.ReactNode }`
    - Import Sidebar and BottomTabBar

    Create src/app/(dashboard)/layout.tsx:
    - 'use client' directive (auth check requires client-side hook)
    - Auth guard pattern (from RESEARCH.md Pattern 3 and Phase 4 decisions):
      ```tsx
      const { isLoggedIn } = useAuth()
      const router = useRouter()
      useEffect(() => {
        if (!isLoggedIn) router.replace('/login')
      }, [isLoggedIn, router])
      if (!isLoggedIn) return null
      ```
    - Renders `<DashboardShell>{children}</DashboardShell>`
    - IMPORTANT: Create the route group directory structure first — mkdir is not needed in Next.js (layout.tsx in (dashboard)/ implicitly creates the group)
    - The (dashboard) route group folder needs to be at: src/app/(dashboard)/layout.tsx
    - This layout applies to all routes nested inside src/app/(dashboard)/

    After creating both files, also create the /dashboard page placeholder to verify the shell works:
    Create src/app/(dashboard)/dashboard/page.tsx as a minimal client component:
    ```tsx
    'use client'
    export default function DashboardPage() {
      return <div className="text-slate-900">Dashboard — loading...</div>
    }
    ```
    This stub will be replaced in Plan 05.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    Build passes. Navigating to /dashboard while logged in shows the dashboard shell (sidebar + content area) covering the global nav. Navigating to /dashboard while logged out redirects to /login.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run build` passes
2. Visiting /dashboard while logged in: DashboardShell visible, global NavShell not visible (covered by fixed overlay)
3. Visiting /dashboard while logged out: immediate redirect to /login
4. Desktop (>768px): sidebar visible on left, main content on right
5. Mobile (≤768px): sidebar hidden, BottomTabBar visible at bottom
6. Sidebar active item highlights correctly on each route
</verification>

<success_criteria>
DashboardShell, Sidebar, BottomTabBar built and wired into route group layout. Auth guard works. Global NavShell correctly suppressed via fixed overlay. Build green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-company-dashboard-api/05-04-SUMMARY.md`
</output>

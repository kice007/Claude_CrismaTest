---
phase: 05-company-dashboard-api
plan: 07
type: execute
wave: 2
depends_on: [05-03, 05-04]
files_modified:
  - src/app/(dashboard)/dashboard/tests/page.tsx
  - src/components/dashboard/TestListTable.tsx
  - src/app/(dashboard)/dashboard/tests/[id]/page.tsx
autonomous: true
requirements: [DASH-06, DATA-04, DATA-05]

must_haves:
  truths:
    - "Tests list loads from GET /api/tests with search by name/role"
    - "Empty state shows when no tests exist with Build New Test CTA"
    - "Test detail shows shareable link, modules, question count, candidate response count"
    - "Edit mode allows updating test name, modules, custom questions with save success modal"
    - "Delete shows confirm modal, on confirm redirects to /dashboard/tests"
    - "Share modal with copy link + send to candidates CTA"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/tests/page.tsx"
      provides: "DASH-07 tests list table"
    - path: "src/components/dashboard/TestListTable.tsx"
      provides: "Tests table with actions column"
    - path: "src/app/(dashboard)/dashboard/tests/[id]/page.tsx"
      provides: "DASH-08 test detail + inline edit + delete + share"
  key_links:
    - from: "src/app/(dashboard)/dashboard/tests/page.tsx"
      to: "/api/tests"
      via: "fetch in useEffect"
      pattern: "fetch.*api/tests"
    - from: "src/app/(dashboard)/dashboard/tests/[id]/page.tsx"
      to: "/api/tests/[id]"
      via: "fetch + PUT + DELETE"
      pattern: "fetch.*api/tests.*id"
---

<objective>
Build the tests list view (DASH-07) and the test detail/edit/delete page (DASH-08).

Purpose: These views complete the test management side of the dashboard. The Build-test wizard (Plan 08) redirects to /dashboard/tests/[newId] on completion — this plan builds that destination.
Output: 2 page files + 1 table component file.
</objective>

<execution_context>
@C:/Users/chris/Downloads/Projects/CrismaTech/CrismTest/crismatest/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/Downloads/Projects/CrismaTech/CrismTest/crismatest/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-company-dashboard-api/05-CONTEXT.md
@.planning/phases/05-company-dashboard-api/05-RESEARCH.md
@.planning/phases/05-company-dashboard-api/05-03-SUMMARY.md
@.planning/phases/05-company-dashboard-api/05-04-SUMMARY.md
@src/components/EmptyState.tsx
@src/components/Skeleton.tsx

<interfaces>
<!-- Test template API response shape (from Plan 03) -->
```typescript
interface TestTemplate {
  id: string
  role: string
  slug: string
  name: string
  modules: string[]
  duration: number
  status: 'active' | 'draft' | 'archived'
  created_at: string
  response_count: number
  shareable_link?: string
  custom_questions?: { text: string; type: string }[]
  questions?: Question[]  // only in /api/tests/[id]
}
```

<!-- Dialog/Drawer modal pattern (established Phase 2) -->
```tsx
import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui/dialog'
// + useMediaQuery for mobile Drawer (vaul) switchover
```

<!-- shadcn Tabs (installed in Plan 01) -->
```tsx
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tests list page + TestListTable</name>
  <files>src/app/(dashboard)/dashboard/tests/page.tsx, src/components/dashboard/TestListTable.tsx</files>
  <action>
    Create src/components/dashboard/TestListTable.tsx:
    - 'use client' directive
    - Props: `{ tests: TestTemplate[], onView: (id: string) => void, onEdit: (id: string) => void, onDelete: (id: string) => void, loading?: boolean }`
    - shadcn Table columns: test name, role (Badge), modules count, created date, status Badge (active=green, draft=amber, archived=slate), response count, actions (3 buttons: eye icon View, pencil icon Edit, trash icon Delete)
    - Each action button calls its respective prop handler with the test id
    - Skeleton: when loading=true, show 5 rows of Skeleton shimmer
    - Row click (non-button area): calls onView(id)

    Create src/app/(dashboard)/dashboard/tests/page.tsx:
    - 'use client' directive
    - State: tests (TestTemplate[]), loading (boolean), search (string), deleteTarget (string | null), deleteModalOpen (boolean)
    - On mount + search change: fetch('/api/tests' + search query param)
    - Page header: "Tests" title + "Build New Test" CTA button (→ router.push('/dashboard/build-test'))
    - Search bar: Input with placeholder from dashboard.tests.search key
    - TestListTable below (pass handlers)
    - Empty state: when tests=[] and !loading, show EmptyState component with:
      - Title from dashboard.tests.empty
      - Description from dashboard.tests.emptyHint
      - CTA: "Build New Test" → router.push('/dashboard/build-test')
    - Delete confirm dialog:
      - Opens when deleteTarget is set
      - Uses shadcn Dialog — title: dashboard.tests.deleteConfirm, body: dashboard.tests.deleteWarning
      - Two buttons: Cancel (closes modal) + Delete Test (calls DELETE /api/tests/[id], on success removes from local state, closes modal)
      - On API error: toast.error(dashboard.errors.loadFailed)
    - onView(id): router.push('/dashboard/tests/' + id)
    - onEdit(id): router.push('/dashboard/tests/' + id) — edit mode triggered by URL or button on detail page
    - All strings via useTranslation() using dashboard.tests keys
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    /dashboard/tests loads test list from API. Search filters by name. Empty state shows with CTA. Delete confirm modal opens on delete action, and on confirm deletes and removes from list. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test detail + edit + share page (/dashboard/tests/[id])</name>
  <files>src/app/(dashboard)/dashboard/tests/[id]/page.tsx</files>
  <action>
    Create src/app/(dashboard)/dashboard/tests/[id]/page.tsx:
    - 'use client' directive
    - Read params: `const { id } = use(params)`
    - State: test (TestTemplate | null), loading (boolean), editMode (boolean), editData ({name: string, modules: string[], customQuestions: {text:string,type:string}[]}), shareModalOpen (boolean), saveSuccessModalOpen (boolean), deleteModalOpen (boolean)
    - On mount: fetch('/api/tests/' + id) — populate test state
    - If 404: show "Test not found" error state with back link

    **View mode layout:**
    - Back link: "← Back to Tests"
    - Header: test name (h1) + role badge + status badge + created date
    - Shareable link field: Input (read-only) displaying test.shareable_link + "Copy Link" button (navigator.clipboard.writeText + toast.success(dashboard.testDetail.copyLinkSuccess))
    - "Share" button → opens share modal
    - Tabs (shadcn Tabs): "Details" tab | "Questions" tab (if questions present)
      - Details tab: modules list (badges/chips for each module), custom questions preview (read-only list)
      - Questions tab: questions list (question text + type badge), max 5 shown with "View all N questions" expand
    - Candidate responses section: response count + "View in Dashboard" link (→ /dashboard with a filter pre-applied, or just /dashboard)
    - Action buttons row: "Edit Test" button (sets editMode=true), "Delete Test" button (opens deleteModalOpen), "Send to Candidates" button (opens email modal with prefilled mailto subject)

    **Edit mode:**
    - When editMode=true: replace view mode header with edit form
    - Editable fields: name (Input), modules (checkboxes or multi-select — use Checkbox per module), custom questions (each question as editable Input + type Select, with add/remove)
    - "Save Changes" button: PUT /api/tests/[id] with editData → on success: opens saveSuccessModal, updates test state, sets editMode=false
    - "Cancel" button: sets editMode=false, resets editData
    - Save success: shadcn Dialog with "Test updated successfully" message + Close button

    **Share modal:**
    - shadcn Dialog (desktop) / Drawer (mobile) — useMediaQuery pattern
    - Copy link button + "Send to Candidates" mailto CTA
    - Follows test-share-modal Pencil frame behavior

    **Delete flow:**
    - Same pattern as tests list page — confirm dialog → DELETE /api/tests/[id] → redirect to /dashboard/tests

    **Send to Candidates email modal:**
    - Small dialog with prefilled mailto href: `mailto:?subject=Test+Invitation&body=Complete+your+assessment+at+${encodeURIComponent(test.shareable_link ?? '')}`
    - Opens in new tab via window.open()

    All strings via useTranslation() using dashboard.testDetail keys.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    /dashboard/tests/[id] loads test detail. Copy link copies to clipboard. Edit mode allows changing name/modules. Save calls PUT endpoint and shows success modal. Delete calls DELETE and redirects to list. Share modal opens with copy + send CTAs. Build passes.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run build` passes
2. /dashboard/tests loads test list from API
3. Search filters tests
4. Delete confirm modal appears, delete removes from list
5. /dashboard/tests/{id} shows test detail with shareable link
6. Edit mode allows updating name/modules, save shows success modal
7. Share modal opens with copy link CTA
</verification>

<success_criteria>
Tests list (DASH-07) and test detail/edit/delete (DASH-08) pages fully functional. All 3 CRUD operations work via API. Build green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-company-dashboard-api/05-07-SUMMARY.md`
</output>

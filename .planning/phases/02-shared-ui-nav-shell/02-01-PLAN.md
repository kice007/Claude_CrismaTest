---
phase: 02-shared-ui-nav-shell
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - components.json
  - src/components/ui/dialog.tsx
  - src/components/ui/drawer.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/sonner.tsx
  - src/lib/utils.ts
  - package.json
autonomous: true
requirements: [UI-03, UI-04, UI-06]

must_haves:
  truths:
    - "npx shadcn@latest add dialog drawer sheet sonner completes without error"
    - "src/components/ui/ directory exists with dialog.tsx, drawer.tsx, sheet.tsx, sonner.tsx"
    - "src/lib/utils.ts exports a cn() function using clsx + tailwind-merge"
    - "globals.css brand tokens are intact after shadcn init (bg-brand-primary still renders #1B4FD8)"
    - "tw-animate-css is imported in globals.css"
  artifacts:
    - path: "components.json"
      provides: "Shadcn configuration (style, tailwind, paths)"
    - path: "src/lib/utils.ts"
      provides: "cn() utility for className composition"
    - path: "src/components/ui/dialog.tsx"
      provides: "Dialog + DialogContent + DialogHeader + DialogTitle"
    - path: "src/components/ui/drawer.tsx"
      provides: "Drawer + DrawerContent + DrawerHeader + DrawerTitle"
    - path: "src/components/ui/sheet.tsx"
      provides: "Sheet + SheetContent + SheetTrigger"
    - path: "src/components/ui/sonner.tsx"
      provides: "Toaster component for layout.tsx"
  key_links:
    - from: "globals.css @theme inline block"
      to: "brand token CSS vars"
      via: "preserved after shadcn init"
      pattern: "--color-brand-primary"
---

<objective>
Bootstrap Shadcn/ui — run init and add the four component primitives Phase 2 depends on.

Purpose: Every subsequent Phase 2 plan (Skeleton, toasts, modals, nav) relies on shadcn components and the cn() utility. This plan unblocks all parallel Wave 2 work.
Output: components.json, src/components/ui/ with dialog/drawer/sheet/sonner, src/lib/utils.ts with cn().
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shared-ui-nav-shell/02-RESEARCH.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run shadcn init and add Phase 2 components</name>
  <files>
    components.json,
    src/lib/utils.ts,
    src/app/globals.css,
    src/components/ui/dialog.tsx,
    src/components/ui/drawer.tsx,
    src/components/ui/sheet.tsx,
    src/components/ui/sonner.tsx,
    package.json
  </files>
  <action>
    Run shadcn init with New York style, slate base color, and globals path src/app/globals.css:

    ```
    npx shadcn@latest init -y --style new-york --base-color slate --css-variables true
    ```

    If the interactive prompt cannot be bypassed with flags, accept: style=new-york, baseColor=slate, globals.css=src/app/globals.css, import alias=@/*.

    Then add the four components needed for Phase 2:

    ```
    npx shadcn@latest add dialog drawer sheet sonner -y
    ```

    After both commands complete, IMMEDIATELY inspect globals.css. Shadcn init overwrites the :root block with generic slate values. The Phase 1 brand tokens MUST be restored. Open globals.css and verify that the following values are present and correct — if any are missing or changed, restore them:

    In :root block (these MUST remain exactly as they are):
    - --brand-primary-raw: #1B4FD8
    - --brand-secondary-raw: #3B6FE8
    - --brand-navy-raw: #0F2A6B
    - --brand-light-raw: #EEF2FF
    - --brand-accent-raw: #6366F1
    - --primary: hsl(221 77% 47%)
    - --secondary: hsl(221 77% 57%)
    - --accent: hsl(239 84% 67%)
    - --destructive: hsl(0 84% 60%)
    - --ring: hsl(221 77% 47%)

    In @theme inline block (these MUST remain exactly as they are):
    - --color-brand-primary: #1B4FD8
    - --color-brand-secondary: #3B6FE8
    - --color-brand-navy: #0F2A6B
    - --color-brand-light: #EEF2FF
    - --color-brand-accent: #6366F1
    - --shadow-card, --shadow-dropdown, --shadow-modal (preserve all three)
    - --radius-card: 0.75rem, --radius-chip: 9999px, --radius-badge: 0.375rem

    If shadcn init added a dark mode block (`.dark { ... }`), leave it — it does not affect v1 (light mode only).

    Verify tw-animate-css is imported: look for `@import "tw-animate-css"` near the top of globals.css. If missing, add it as the second line (after `@import "tailwindcss"`).

    Do NOT modify src/lib/utils.ts manually — shadcn generates it. Only inspect it to confirm cn() is exported.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - components.json exists at project root
    - src/lib/utils.ts exports cn()
    - src/components/ui/dialog.tsx, drawer.tsx, sheet.tsx, sonner.tsx all exist
    - globals.css has @import "tw-animate-css" and all Phase 1 brand tokens intact
    - npm run build exits with code 0
  </done>
</task>

</tasks>

<verification>
After completing the task, run:

```
npm run build
```

Then manually check src/app/globals.css to confirm --color-brand-primary is still #1B4FD8 and --primary is hsl(221 77% 47%).
</verification>

<success_criteria>
- Shadcn/ui is installed and configured
- cn() utility available at @/lib/utils
- Dialog, Drawer, Sheet, Sonner components scaffolded in src/components/ui/
- All Phase 1 brand tokens preserved in globals.css
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-ui-nav-shell/02-01-SUMMARY.md`
</output>

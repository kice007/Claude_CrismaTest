---
phase: 02-shared-ui-nav-shell
plan: "06"
type: execute
wave: 5
depends_on: ["02-02", "02-03", "02-04", "02-05"]
files_modified:
  - src/app/dev/design-system/page.tsx
  - public/locales/en/translation.json
  - public/locales/fr/translation.json
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UI-07, LAND-06, LAND-07, LAND-08]

must_haves:
  truths:
    - "Visiting /dev/design-system shows a Phase 2 section with live component demos"
    - "Skeleton demos show shimmer rectangles of different sizes"
    - "EmptyState demo renders the card with SVG illustration, title, body, and CTA"
    - "Toast trigger buttons fire success, warning, and error toasts"
    - "Modal trigger buttons open InviteModal, CalendarModal, ExportModal, ContactModal"
    - "On desktop, modals open as centered Dialog; on mobile, as bottom Drawer"
    - "Nav is visible above the design-system page content"
  artifacts:
    - path: "src/app/dev/design-system/page.tsx"
      provides: "Updated dev page with Phase 2 component section"
    - path: "public/locales/en/translation.json"
      provides: "All Phase 2 i18n keys including nav strings"
    - path: "public/locales/fr/translation.json"
      provides: "French translations for all Phase 2 i18n keys"
  key_links:
    - from: "src/app/dev/design-system/page.tsx"
      to: "src/components/Skeleton.tsx"
      via: "import Skeleton"
      pattern: "Skeleton"
    - from: "src/app/dev/design-system/page.tsx"
      to: "src/components/EmptyState.tsx"
      via: "import EmptyState"
      pattern: "EmptyState"
    - from: "src/app/dev/design-system/page.tsx"
      to: "src/components/modals/"
      via: "import InviteModal, CalendarModal, ExportModal, ContactModal"
      pattern: "Modal"
---

<objective>
Update the /dev/design-system validation page with Phase 2 component demos and add all missing i18n translation keys.

Purpose: The design-system page is the manual verification surface for all Phase 2 components. It also serves as a catch-all to ensure i18n keys added in Plans 01–05 are complete in both locale files. This plan ends with a human verification checkpoint.
Output: Updated design-system page, complete translation files, human checkpoint confirming all Phase 2 components work.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-ui-nav-shell/02-CONTEXT.md
@src/app/dev/design-system/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and complete i18n translation files</name>
  <files>
    public/locales/en/translation.json,
    public/locales/fr/translation.json
  </files>
  <action>
    Read the current en/translation.json and fr/translation.json files. Then audit ALL Phase 2 components for useTranslation() calls to find every key that was added across Plans 01–05.

    Keys added by Plan 05 (nav strings) that MUST be present:
    ```json
    "nav_for_candidates": "For Candidates",
    "nav_for_companies": "For Companies",
    "nav_pricing": "Pricing",
    "nav_login": "Login",
    "nav_sign_up": "Sign Up",
    "nav_open_menu": "Open menu",
    "nav_mobile_title": "Navigation"
    ```

    French equivalents:
    ```json
    "nav_for_candidates": "Pour les candidats",
    "nav_for_companies": "Pour les entreprises",
    "nav_pricing": "Tarifs",
    "nav_login": "Connexion",
    "nav_sign_up": "S'inscrire",
    "nav_open_menu": "Ouvrir le menu",
    "nav_mobile_title": "Navigation"
    ```

    Also add keys for the design-system page's Phase 2 section (used in Task 2):
    ```json
    "design_system_phase2_title": "Phase 2 — Shared UI Components",
    "design_system_skeleton_title": "Skeleton",
    "design_system_empty_state_title": "Empty State",
    "design_system_toasts_title": "Toasts",
    "design_system_modals_title": "Modals",
    "design_system_toast_success": "Show Success Toast",
    "design_system_toast_warning": "Show Warning Toast",
    "design_system_toast_error": "Show Error Toast"
    ```

    French for design-system keys:
    ```json
    "design_system_phase2_title": "Phase 2 — Composants UI partagés",
    "design_system_skeleton_title": "Squelettes",
    "design_system_empty_state_title": "État vide",
    "design_system_toasts_title": "Notifications",
    "design_system_modals_title": "Modales",
    "design_system_toast_success": "Afficher succès",
    "design_system_toast_warning": "Afficher avertissement",
    "design_system_toast_error": "Afficher erreur"
    ```

    Merge these keys INTO the existing JSON files — do NOT replace existing Phase 1 keys. Add them as additional properties in the root object. Preserve the existing key order (add new keys at the end).
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>
    - en/translation.json and fr/translation.json both contain all Phase 2 nav keys and design-system keys
    - No existing Phase 1 keys removed
    - Valid JSON (no trailing commas, no syntax errors)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 2 section to design-system page</name>
  <files>src/app/dev/design-system/page.tsx</files>
  <action>
    Read the existing design-system page. Append a "Phase 2" section at the end of the existing content (before the closing `</div>` of the main container). Do NOT remove or modify any Phase 1 sections.

    The Phase 2 section should include:

    1. SECTION HEADER: `<h2>` with t("design_system_phase2_title") in brand-navy

    2. SKELETON DEMOS (t("design_system_skeleton_title")):
       ```tsx
       import { Skeleton } from "@/components/Skeleton";

       // Demo: a text line skeleton, a card skeleton, an avatar skeleton
       <div className="space-y-3">
         <Skeleton className="h-4 w-48" />
         <Skeleton className="h-4 w-64" />
         <Skeleton className="h-32 w-full rounded-[var(--radius-card)]" />
         <div className="flex gap-3">
           <Skeleton className="h-10 w-10 rounded-full" />
           <div className="space-y-2 flex-1">
             <Skeleton className="h-4 w-32" />
             <Skeleton className="h-3 w-24" />
           </div>
         </div>
       </div>
       ```

    3. EMPTY STATE DEMO (t("design_system_empty_state_title")):
       ```tsx
       import { EmptyState } from "@/components/EmptyState";

       <EmptyState
         title="No candidates yet"
         body="Invite candidates to take a test and their results will appear here."
         ctaLabel="Invite Candidate"
         ctaHref="#"
       />
       ```

    4. TOAST DEMOS (t("design_system_toasts_title")):
       Three buttons that call toast.success(), toast.warning(), toast.error():
       ```tsx
       import { toast } from "sonner";

       <div className="flex flex-wrap gap-3">
         <button onClick={() => toast.success("Candidate invited successfully")}
           className="min-h-[48px] px-4 bg-success text-white rounded-md text-sm font-medium">
           {t("design_system_toast_success")}
         </button>
         <button onClick={() => toast.warning("Session expiring soon")}
           className="min-h-[48px] px-4 bg-warning text-white rounded-md text-sm font-medium">
           {t("design_system_toast_warning")}
         </button>
         <button onClick={() => toast.error("Failed to send invite")}
           className="min-h-[48px] px-4 bg-danger text-white rounded-md text-sm font-medium">
           {t("design_system_toast_error")}
         </button>
       </div>
       ```

    5. MODAL DEMOS (t("design_system_modals_title")):
       Four buttons that open the four modal variants. Use useState for each modal's open state:
       ```tsx
       import { useState } from "react";
       import { InviteModal } from "@/components/modals/InviteModal";
       import { CalendarModal } from "@/components/modals/CalendarModal";
       import { ExportModal } from "@/components/modals/ExportModal";
       import { ContactModal } from "@/components/modals/ContactModal";

       const [inviteOpen, setInviteOpen] = useState(false);
       const [calendarOpen, setCalendarOpen] = useState(false);
       const [exportOpen, setExportOpen] = useState(false);
       const [contactOpen, setContactOpen] = useState(false);

       // Four trigger buttons (each min-h-[48px], bg-brand-primary style)
       // Each modal component mounted at bottom of section with its open/onOpenChange props
       ```

    Wrap each section in a `<section className="space-y-4">` with a `<h3>` heading. Use `<div className="p-6 bg-card rounded-[var(--radius-card)] shadow-[var(--shadow-card)]">` as the demo container for each section.

    The page is already `"use client"` and imports are already at the top — add the new imports to the existing import block.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - /dev/design-system page has a Phase 2 section with skeleton, empty state, toast, and modal demos
    - All Phase 1 sections intact
    - All new strings go through useTranslation()
    - Build passes with zero errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of all Phase 2 components</name>
  <what-built>
    All Phase 2 components: Shadcn/ui init, Skeleton shimmer, EmptyState + 404/500 pages, Sonner toast system, four responsive modals (Dialog on desktop / Drawer on mobile), responsive nav shell with glassmorphism scroll transition.
  </what-built>
  <how-to-verify>
    Start the dev server: `npm run dev`

    Visit http://localhost:3000/dev/design-system and verify:

    1. NAV: Logo + links + language switcher + Login + Sign Up appear at top. Scroll down — shadow and background blur increase. Switch language — nav links change language without page reload.

    2. SKELETON: Three skeleton demo shapes appear with shimmer animation. Enable "Reduce motion" in OS accessibility settings — shimmer stops and boxes show as static muted color.

    3. EMPTY STATE: Card appears with abstract SVG illustration, "No candidates yet" title, body text, and "Invite Candidate" CTA button.

    4. TOASTS: Click each of the three toast trigger buttons — success (green), warning (amber), error (red) toasts appear at bottom-right. Each auto-dismisses after 4 seconds. The × button on each toast closes it early.

    5. MODALS: Click each modal trigger button:
       - On desktop (≥768px): centered Dialog appears with backdrop
       - On mobile (<768px, use DevTools): bottom Drawer slides up
       - Press ESC to close
       - Click outside/backdrop to close

    6. MOBILE NAV: Resize to 320px. Hamburger appears. Click it — Sheet slides down with all links and language toggle. All tap targets appear ≥48px tall.

    7. HORIZONTAL OVERFLOW: At 320px, confirm no horizontal scrollbar appears on any page.

    8. 404 PAGE: Visit http://localhost:3000/this-page-does-not-exist — branded EmptyState appears (not Next.js default 404).
  </how-to-verify>
  <action>Run `npm run dev` and perform the manual verification steps listed in how-to-verify above.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>User has typed "approved" confirming all Phase 2 component behaviors are correct.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human checkpoint is the final verification gate for all Phase 2 requirements. The checkpoint covers all 10 requirement IDs: UI-01 through UI-07, LAND-06, LAND-07, LAND-08.
</verification>

<success_criteria>
Phase 2 is complete when:
- Skeleton shimmer renders and respects reduced-motion (UI-01, UI-06)
- EmptyState renders with SVG, text, CTA (UI-02)
- Toast success/warning/error work with auto-dismiss and manual close (UI-03)
- Modals are Dialog on desktop, Drawer on mobile, focus-trapped, ESC closes (UI-04)
- 404 and 500 pages show branded EmptyState (UI-05)
- All animations respect reduced-motion (UI-06)
- All interactive elements meet 48px touch targets, 16px form inputs (UI-07)
- Fixed nav with glassmorphism scroll transition (LAND-06)
- Mobile hamburger sheet with language toggle (LAND-07)
- No horizontal overflow at 320px (LAND-08)
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-ui-nav-shell/02-06-SUMMARY.md`
</output>
